# memory-engine Stabilization Plan

> **Status: Current product roadmap**
>
> `docs/current-state.md` is the sole owner of the exact current product state and next authorized decision. This roadmap describes the accepted product sequence and branching logic only; it does not authorize execution or describe a particular deployment.

## Current product sequence

### NOW

#### memory-engine 1.1 — Recall Quality

The accepted post-1.0 product line is **memory-engine 1.1 — Recall Quality**. The primary bottleneck has moved from storage and authority boundaries to recall triggering, query scope understanding, cross-session evidence composition, and ranking complete/current evidence into the production `topK=3` budget. Keep these boundaries distinct: memory existence, recall attempt, candidate generation, production top-K entry, and answering-model use are separate failure points.

**Q2-B2 is `PASS_WITH_FINDINGS / CLOSED`; the 1.1 Q3 architecture direction is `ACCEPTED_WITH_LIMITATIONS / OFFLINE SOURCE CLOSED`; R3-C0 control runtime qualification is closed; R3-C1-A-E0/E1 prove strong 8B quality but fail the frozen interactive latency/reliability SLA; M1 model attribution is source-closed; M2 0.6B execution is complete with aggregate quality/latency/reliability passing but two strict gates failing; and M3 offline attribution is complete.** QA0 source blockers no longer block the recall-quality sequence; Q0 is `PASS_WITH_FINDINGS / CLOSED`; the Q1 product metric contract is closed and the current lexical baseline is frozen; Q2-A1/A2 are closed; and Q2-B1 has frozen the source-only semantic binding contract. The accepted Q3 direction retains an independent relevance-rerank layer on fixed eligible candidates, with same-profile control fallback. M2 must not be retroactively re-scored by changing its frozen gates; the next bounded step is prospective qualification-contract-v2 / disjoint-holdout design, with no provider execution authorized.

**Q3 wiring follow-up — 2026-09-12 source closure and production-shaped profile freeze:**

1. The incremental-review cleanup is `PASS_WITH_FINDINGS / SOURCE CLOSED` at `b6cdf5a0f959b5ac38266dd4ebc368d0c57605a4`: eligibility/canonical/final-serving accounting is stage-separated, normal topK truncation is not canonical failure, the reranker callback-index cleanup is behavior-preserving, and empty-text plus managed/external archive semantics are fixture-frozen.
2. R3 explicit-search bounded valid-pool serving is `PASS_WITH_FINDINGS / SOURCE CLOSED` at `298627c13c14b69c5997db7942c8ed01c87154ac`. The default-off trusted profile is limited to dedicated `memory_engine_search` and legacy `memory_engine action=search`; AutoRecall and unrelated Hybrid callers remain outside the propagation boundary. Enabled serving uses one bounded fused pool, canonical validation, same-pool control/rerank and take-topK with no refill outside candidateDepth. Canonical batch/projection failures fail closed before adapter disclosure; adapter failure is atomic same-pool control fallback.
3. Production-shaped R3 profile v1 is runtime-qualified in C0 control mode for the current product `topK=3`: `candidateDepth=20`, `maxCodePointsPerCandidate=4000`, `maxTotalCodePoints=48000`, `deadlineMs=2500`, no refill, same-pool fallback. These values are not proof of optimal quality and do not authorize a real provider. Existing FTS/vector collection depths (`20`/`30`) make a 20-candidate pool reachable without changing candidate generation. Code-point budgets are not provider token budgets.
4. Preserve the frozen taxonomy: LME `RANK_MISS=248/419` already means complete gold is in the historical top50 but not fully in top3 (217 budget-feasible; 31 require more than three evidence items). Do not reinterpret those cases under the new depth-20 profile without separately attributed evidence, and do not reopen benchmark/provider work to optimize the profile before qualification.

**R3-C0-O1 lifecycle closeout — `CLOSED`; R3-C0 — `CONTROL RUNTIME QUALIFIED`.** The final production deployment is source `5f263b3d5a4aab7480b7d7e55c917b11d786842e`, candidate artifact `0a6edaf4688eebeaabccd9dcee888db6889711eb758befa9710d82d00eb98fc9`, runtime `7f2e80d3f4887fd4b3db41afc5d0e8b60b96c16631674589d38b4c6a843b6272`, and native SHA-256 `be4109c5b07514ade1a2e1452cbed9fca25cbb8d025b76fa2a81e21a91286a05`.

1. **Installed artifact gate — `PASS`.** OpenClaw installation was `SPLIT_ONLY`, `accepted=true`, policy `allow_internal_hardlink_split_v1`, stop reason `accepted_controlled_internal_hardlink_split_v1`; candidate/installed semantic identity remained `e556b82c2d67487b8c4166000603a43de610694cf3d169c6205495b2739854ae`, with internal hardlink groups normalized `3→0` and no external references or validation errors.
2. **Disabled production state — `PASS`.** `memory-engine enabled=true`, `AutoRecall=false`, `topK=3`, and `explicitSearchRerankControl` absent. Gateway was healthy with `NRestarts=0`; the final disabled production stability window passed for 60 seconds. Normalized config equality was true. Raw config SHA changed only because OpenClaw auto-managed `meta.lastTouchedAt` changed; this is not semantic config drift.
3. **Data and telemetry adjudication — `PASS`.** Engine identity is `2d642bdb90a3b374e4a899639534bf83aaee5f11493d49cf6361649de6d685fb`, `memory_events.max(id)=369`, `row_count=321`, and the final deployment added no events. C0 observations `366/367/368/369` remain preserved. Lance identity is `30ecfcbeb8460fd409f84c8a9ecdc3ca9effd7f705f17fe34a2d17ec59033e94` and remained unchanged. Provider calls, benchmarks, and C1 were all zero.

The earlier `4bff77a` delivery and disabled-host qualification remain historical evidence. The qualification-line false failures are archived as harness/operator failures—not memory-engine product defects, installer defects, or R3 architecture defects—including incorrect topology/v1 equality, a nonexistent comparator nested field, `.result` versus authoritative `.output.results`, full Lance manifest byte comparison including `checked_at`, and raw config SHA equality despite an OpenClaw auto-stamp.

**R3-C1-A-I0 source closure — `PASS / SOURCE IMPLEMENTED / VERIFIED` at `1324c8c05e62639ae3418b64b6e4e04c14c51adb`.** The isolated C1-A tooling freezes the 1970-case LoCoMo source population into `P256/D64/S8`, preserves the FTS-only/non-production-hybrid evidence limitation, binds canonical depth-20 `4000/48000` projection, uses a fail-closed SiliconFlow `Qwen/Qwen3-Reranker-8B` adapter with local token preflight, and separates zero-provider `prepare` from packet-bound `execute-provider`.

**R3-C1-A-E0 — `FAIL_WITH_FINDINGS / EXECUTION COMPLETE`; quality `PASS`, 2500ms reliability `FAIL`.** The exact bounded run completed all `328/328` authorized requests. P256 Recall-all@3 improved `44.92%→63.67%` (`+18.75pp`), Recall-any@3 `52.73%→73.83%` (`+21.09pp`), evidence coverage@3 `48.16%→68.10%` (`+19.93pp`), and protected-control regression was `1.74%`. P256 reliability was `218/256 applied` and `38/256 fallback`; every fallback was a hard timeout at the `2500ms` boundary. There were no 429, HTTP, structural-response, unauthorized-transmission, retry, token-budget or cost-cap failures. Successful-request p95/p99 were `1974/2373ms`, but the timeout population makes those values right-censored for product-tail adjudication.

**R3-C1-A-L1 deadline revision — `PASS / SOURCE IMPLEMENTED / VERIFIED` at `3228d5e5662a9d8a8b4318f5c8ad9af69f1c8bd9`.** The C1-A qualification profile advances to `r3_c1a_locomo_fts20_canonical_v2` and changes only the hard deadline `2500→5000ms`; depth, text/token budgets, provider binding, fallback semantics and all acceptance thresholds remain unchanged. Node24 C1/C0 adjacent tests are `65/65 PASS`; static check `759`, integrity `346/0`, OpenSpec strict `12/12`.

**R3-C1-A-E1 — `FAIL_WITH_FINDINGS / EXECUTION COMPLETE`; quality `PASS`, real provider latency/reliability `FAIL`.** The exact v2 run completed all `328/328` authorized requests at source `dae3aa20105403b1acbbb787720f921d4c844f06`, manifest `4f353270a15bcadc269748d95e19f281b14a9a93ebdc86dd0c6272472e522c7a`. P256 Recall-all@3 improved `44.92%→64.06%` (`+19.14pp`), Recall-any@3 `52.73%→75.39%` (`+22.66pp`), evidence coverage@3 `48.16%→69.01%` (`+20.84pp`), and protected-control regression remained `1.74%`. Reliability improved to `242/256 applied`, `14/256 fallback`, but still missed the `>=99% / <=1%` gates. Successful-request p95/p99 were `2620/4267ms`, both above the unchanged `2000/2400ms` SLA gates, proving that the remaining problem is not only the hard timeout censoring. Main-run fallbacks remained timeout-only; sentinel exact applied-pair top3 stability was `7/8`, with the single failed pair caused by timeout rather than observed order drift.

**R3-C1-A-M1 model-attribution source work — `PASS / SOURCE IMPLEMENTED / VERIFIED / CLOSED` at `d60a582c9f4e32a49fce16852de3755b2403f165`.** The qualification surface retains 8B as the default and adds exactly one explicit alternate model, `Qwen/Qwen3-Reranker-0.6B`. Arbitrary model strings and `Qwen/Qwen3-Reranker-4B` are not accepted. The selected model is bound into the frozen manifest; execution regenerates from that exact manifest-bound model and requires packet equality. Production configuration remains untouched. Node24 C1/C0 adjacent tests are `69/69 PASS`; static check `759`, integrity `346/0`, OpenSpec strict `12/12`, and `git diff --check` pass.

**R3-C1-A-M2 — `FAIL_WITH_FINDINGS / EXECUTION COMPLETE`.** The exact 0.6B run used source `c833df33172c21f35bd420b4e34f34500d3a7832`, manifest `c723886df9083bdd72753059afc4773195707d96077946b384eb163df725b272`, the same frozen P256/D64/S8 populations and unchanged thresholds. P256 Recall-all@3 improved `+18.75pp`, Recall-any@3 `+22.27pp`, evidence coverage@3 `+20.81pp`; all `256/256` primary requests applied with `0` fallback and p95/p99/max `612/640/668ms`. Protect regression was `4/115 = 3.48%` versus the frozen `<=3%` gate. Sentinel execution was `8/8` applied pairs and `7/8` exact ordered-top3; the single mismatch preserved the same top3 set and top1 and only swapped ranks 2/3. All `328/328` requests completed within authorization and budget, with accounted cost about `$0.0253`.

**R3-C1-A-M3 — `PASS / OFFLINE ATTRIBUTION COMPLETE`.** Two of the four M2 protect regressions are category-5 adversarial cases whose premise-conflicting frozen evidence causes a strict retrieval penalty even when the selected text better matches the literal question subject. The non-adversarial explanatory slice is `2/79 = 2.53%`, but that is post-hoc and does not replace the frozen M2 gate. Of the two normal regressions, one (`conv-50:qa:129`) retains answer-equivalent tour/performance/crowd evidence through a different chunk; the other (`conv-50:qa:53`) genuinely loses one of two required support facts, and 8B also loses that case. Restricting comparison to `242` cases where both E1 8B and M2 0.6B truly applied gives `235` Recall-all ties, `5` 8B wins and `2` 0.6B wins; mean evidence coverage is `0.6976` vs `0.6889`.

**R3-C1-A-M4 qualification-contract-v2 source work — `PASS / SOURCE CONTRACT IMPLEMENTED / VERIFIED / CLOSED` at `381b0e2630ae3ea7488e7e79787a7433878d2b77`.** The isolated v2 contract verifies the prior v1 manifest body hash, excludes exactly the observed `320` unique P256+D64 cases, then deterministically draws a disjoint `H256 + D64` from the remaining `1650` cases and `S16` from eligible H256. The real-corpus zero-provider smoke has `overlap=0`, `H256 eligible=256/256`, and category counts `28/47/11/106/64` for categories 1–5. Aggregate quality/reliability/latency thresholds remain unchanged; protect regression is gated prospectively on answer-bearing categories 1–4 while category 5 remains in aggregate metrics and is reported separately; sentinel blockers are `16/16` applied pairs, exact top1 `16/16`, and exact top3 membership `16/16`, with exact ordered-top3 retained as a diagnostic. Focused v2 tests are `4/4 PASS`; C1/C0 adjacent tests `78/78 PASS`; static check `761`, integrity `347/0`, OpenSpec strict `12/12`, and `git diff --check` pass. No provider request occurred.

**R3-C1-A-M5 v2 execution seam — `PASS / SOURCE IMPLEMENTED / VERIFIED / CLOSED` at `ac9eb2405d8060f60fe5b034b817f55d187daff6`; provider execution remains unauthorized.** M5 adds a separate v2 packet validator, batch and execution scorer path rather than changing the v1 `328`-request contract. The path binds the exact source, v2 manifest hash and prior observed M2 manifest hash to `SiliconFlow / Qwen/Qwen3-Reranker-0.6B`, `5000ms`, `6M` input tokens, `$1`, execution_count `1`, explicit egress ALLOW and an exact `336 = H256 + D64 + S16` request envelope. It recomputes the v2 manifest body hash, verifies `256/64/16` structure and sentinel⊂primary, and fake execution consumes all `336` requests through the v2 scorer seam. Focused M4+M5 tests are `7/7 PASS`; C1/C0 adjacent tests `92/92 PASS`; static check `765`, integrity `348/0`, OpenSpec strict `12/12`, and `git diff --check` pass. No CLI/operator entry or provider request was added.

**R3-C1-A-M6 v2 operator/CLI — `PASS / SOURCE IMPLEMENTED / VERIFIED / CLOSED` at `ab4b20d8423b55ea36baaa37649a7e7ca0b9e668`.** The committed zero-provider CLI exposes `prepare`, `freeze-packet`, and `validate`; production defaults pin the prior observed corpus to exact M2 manifest `c723886df9083bdd72753059afc4773195707d96077946b384eb163df725b272`. `prepare` requires a clean source plus explicit egress ALLOW, while `freeze-packet` requires current price, rate-limit source, pacing, execution root and API-key env as explicit inputs. M6 does not generate a real ALLOW holdout manifest/packet because new-corpus egress remains unauthorized. Focused M4/M5/M6 tests are `12/12 PASS`; C1/C0 adjacent tests `97/97 PASS`; static check `766`, integrity `349/0`, OpenSpec strict `12/12`, and `git diff --check` pass.

**R3-C1-A-M7 real disjoint-holdout provider qualification — `FAIL_WITH_FINDINGS / EXECUTION COMPLETE`.** Exact source `924b01b2cdf03b916b8814e29fb2ad73d3b5ddc9`, manifest `fbe0076478526cbe1b2e328d3297cc146d030650bac766d281b62c988345d5dd`, prior observed M2 manifest `c723886df9083bdd72753059afc4773195707d96077946b384eb163df725b272`. The one authorized run completed `336/336` requests with no stopped artifact, fallback, retry, structural invalidity, unauthorized transmission or budget/integrity failure. H256 Recall-all@3 improved `+17.58pp`, Recall-any@3 `+19.92pp`, evidence coverage@3 `+18.85pp`; non-adversarial protect regression was `1/83 = 1.20%`. Latency p95/p99/max was `582/620/651ms`. S16 applied pairs and top1 stability both passed `16/16`, while frozen top3-set stability failed `15/16`; `conv-47:qa:150` changed only the third member. Accounted cost was about `$0.0264`. **Owner accepts SiliconFlow `Qwen/Qwen3-Reranker-0.6B` for the C1 explicit-search rerank line with this known nondeterminism limitation; M7 remains historically `FAIL_WITH_FINDINGS`.**

**C1-B minimal real-host source seam — `PASS / SOURCE IMPLEMENTED / VERIFIED / COMMIT PENDING`; real-host execution remains unauthorized.** Keep this experimental: the seam adds only a sibling trusted switch `explicitSearchRerankProvider.enabled`, a fixed SiliconFlow `Qwen/Qwen3-Reranker-0.6B` policy, and index wiring through the existing canonical rerank path. C0 control config is unchanged; simultaneous control/provider enablement fails config validation. The host shape is `candidateDepth=20`, canonical `4000/48000`, `deadlineMs=2500`. Focused tests pass `35/35`, broader config/runtime/tool integration `59/59`, static `768`, integrity `350/0`, OpenSpec strict `12/12`, diff-check PASS.

**Next product decision — C1-B real-host smoke: `NOT STARTED / NOT AUTHORIZED`.** Do not build another benchmark framework. A later bounded smoke only needs to show that real `memory_engine_search` reaches SiliconFlow 0.6B, returns a reranked top3, preserves same-pool fallback on error, and leaves AutoRecall off. Production rerank activation, broader Gateway/DB/LanceDB mutation, push and tag remain unauthorized.

QA0 keeps machine-executable acceptance as the governing rule rather than treating documentation volume or a green `node --test` summary as evidence by itself:

- adopt one repository-owned review-gate command as the acceptance entry point. It must compose `npm run check`, the behavior/schema/CLI/runtime tests required by the changed surface, applicable OpenSpec strict validation, `git diff --check`, and a test-integrity check. `npm run check` remains a syntax/parser subcheck and must not be represented as a standalone quality gate;
- make empty-shell and token/phase-string-only tests fail the integrity gate unless they are explicitly classified as non-test fixtures/helpers. Re-baseline the current tree with a frozen detector, replace or remove existing shell tests, and prohibit new ones from inflating the pass count. Behavior, schema, link/index, CLI, and real runtime-artifact tests remain valid evidence when they assert observable contracts;
- hard-bound untrusted retrieval breadth. Every production/public path that accepts `top_k` must validate a finite integer range before SQL/vector work, share one production-owned maximum, and keep schema/runtime validation consistent. Invalid or oversized values fail closed rather than flowing into retrieval. Benchmark-owned limits such as AML `top_k<=100` remain benchmark-local and do not define the production maximum;
- Planner acceptance must be based on fresh command output from the executable gate plus independent DevSpace spot-checks of HEAD/status/diff and the affected contract. Narrative reports, historical PASS text, or test-file counts cannot substitute for the gate result;
- mutation paths require exact `memory_id`/`canonical_id`/chunk identity and fail closed on ambiguous prefixes; `cite` is not unrestricted reinforcement authority and must be bound to the current search result set or remain withheld;
- reinforcement evaluates the current decayed state and never clears `conflict_flag`; checkpoint confidence binds to the exact entry/chunk rather than the latest path row;
- secret/path/hash-like values are not routed as preferences; Add distinguishes `committed`, `partial`, `pending`, and `failed`, and vectorization covers all new chunks;
- stale schema/compatibility instructions do not authorize Core writes, and Core ownership remains read-only from memory-engine;
- structured event-time remains bounded: `memory_event_times` is an Engine-owned schema MVP/proposed sidecar, default writes are `denied_by_default_write_guard`, and it is not integrated with recall, checkpoint, or ranking. Exact `event_at` requires trustworthy evidence; `updated_at`, file mtime, batch/import time, and smart-add path dates are not substitutes. `date_only` and `unknown` are valid states, so exact event-time coverage and temporal-channel qualification remain unproven.

The 2026-09-02 GitHub audit remains a finding source, not a second roadmap. Its six former pre-Q0 blockers are now **historical QA0 source findings that have been addressed on the local development line** and remain useful regression contracts:

1. **Archived-vector exclusion** — managed candidates with `is_archived=1` must be rejected before vector candidates enter fusion/rerank/final results, with channel-level regression coverage.
2. **KG intra-channel ranking correctness** — KG candidates must be ordered by the intended query relevance/structured-match score before truncation and RRF rank assignment; `updated_at` must not silently determine KG rank after relevance features are computed.
3. **Canonical smart-add writer/fingerprint contract** — tool and checkpoint writers must share one fingerprint canonicalization and compatible entry contract; duplicate-detection failures must not silently become `not duplicate` when that would permit an unsafe duplicate write.
4. **Single business-date/time-zone contract** — root/tool/checkpoint/bin date handling must resolve the same configured business time zone and day boundary; remove or reduce duplicate implementations after compatibility coverage proves equivalence.
5. **Legacy attached-Core capability retirement** — production/trusted business paths should converge on `isolated-dbs.js`; the legacy `engine-db.js` attached-Core path and regex guard must not remain an alternate Core-write authority. Do not attempt to solve this by making the regex parser more elaborate.
6. **Invalid-config observability** — normalization errors for retrieval/AutoRecall settings must reach a stable startup/health/log surface; critical retrieval settings must not silently appear accepted while executing defaults.

The Console local-access boundary is now **`SOURCE CLOSED / REVIEW-GATE PASS`** at commit `02882793658a0704ead5285e9e49a1e307718287`: dynamic Console pages and APIs require a configured token, Host validation fails closed against DNS-rebinding names, cross-site/mismatched-Origin requests are rejected, and wildcard binding requires an explicit finite allowed-host list. This is source evidence only; no Console deployment/runtime activation is inferred. One security hardening remains an AutoRecall activation blocker:

- **Untrusted-memory prompt boundary** — raw recalled memory and checkpoint raw-log material must be clearly represented as untrusted data rather than instructions. This is a hard blocker before any broader AutoRecall enablement; with `AutoRecall=false`, it does not by itself reopen runtime rollout.

Audit performance findings such as full-table candidate metadata loading, confidence backfill cost, event-write DDL overhead, channel serialization, and Console aggregation remain **measure-first work**. Q1 freezes p50/p95 and small-budget quality metrics; Q2 can then attribute latency/call-cost by channel. Do not rewrite retrieval hot paths solely from static complexity claims without measured evidence.

The audit claim that `lib/runtime-authority/` must be wired into `before_prompt_build` is **rejected as a roadmap requirement**. That subsystem is retained as candidate/runtime qualification tooling unless a later product decision proves a different role; its lack of conversational-runtime imports is not by itself an authorization bug. Likewise TypeScript/Prettier adoption is not a QA0 requirement. GitHub CI remains useful release/repository hygiene and may be added independently once the executable `npm run review` contract is stable.

QA0 does not create a new `memory_entries` aggregate, broadly retune ranking/vector thresholds, rewrite the retrieval pipeline, activate event-time writes, or mutate live DB/Gateway/config/LanceDB/plugin state. `docs/current-state.md` remains the single exact-state authority, historical reports remain evidence/reference only, and the executable repository gate remains the enforcement mechanism.

**Current repair plan from the 2026-09-05 architecture/code review:**

1. **R1 Tool ID/result correctness — `COMMITTED / SOURCE CLOSED / Level A`; R1.1 indexed literal-prefix follow-up also `SOURCE CLOSED / REVIEW-GATE PASS`.** Commit `b4f365647d2071e6e5cb8ea8d37ea0e1454eb70b` replaced SQL `LIKE prefix%` semantics in `memory_engine update` and Owner-only `memory_engine_get` with literal exact-or-unique-prefix semantics; exact IDs take precedence, ambiguity/nonexistence fail closed, `%` / `_` are literal ID characters, and the fabricated aggregate `cite.next_confidence` was removed. Follow-up commit `02882793658a0704ead5285e9e49a1e307718287` removes the `substr(...)=?` prefix-scan regression by sharing one indexed literal-prefix resolver across update/get/reinforcement paths. Parameterized `GLOB ?` was verified with SQLite `EXPLAIN QUERY PLAN` to use the primary-key index while preserving literal `%`, `_`, `*`, `?`, and `[` semantics. The Node 24 full repository review at that source state recorded `2489` pass / `0` fail / `12` skip, OpenSpec strict `12/12`, and overall `PASS`. R1/R1.1 change no retrieval ranking, benchmark metric definition, runtime activation, or live data.
2. **R2 Smart-add concurrent writer safety — `BACKLOG / CONTROLLED CHANGE`.** The canonical fingerprint contract is retained, but read-check-append has a TOCTOU race. Choose the smallest single-writer/lock mechanism that covers the real process topology and add concurrency regression evidence. Because this touches persistent write semantics, implementation requires its own bounded change and does not block the Q3 architecture decision.
3. **R3 Canonical valid-topK serving — `PASS_WITH_FINDINGS / SOURCE CLOSED`; disabled real-host compatibility `PASS / CLOSED`; R3-C0-O1 `CLOSED`; R3-C0 `CONTROL RUNTIME QUALIFIED`; R3-C1-A-E0/E1 `EXECUTION COMPLETE / QUALITY PASS / PROVIDER LATENCY-RELIABILITY FAIL`; R3-C1-A-M1 `PASS / SOURCE IMPLEMENTED / VERIFIED / CLOSED` at `d60a582c9f4e32a49fce16852de3755b2403f165`; R3-C1-A-M2 `FAIL_WITH_FINDINGS / EXECUTION COMPLETE`; R3-C1-A-M3 `PASS / OFFLINE ATTRIBUTION COMPLETE`.** The accepted explicit-search profile is default-off and source-closed at `298627c13c14b69c5997db7942c8ed01c87154ac`: bounded fused pool → canonical validate → same-pool control/rerank → take K, with no refill outside candidateDepth. C0's production-shaped control profile remains qualified at live `topK=3`, `candidateDepth=20`, `4000` code points per candidate, `48000` total code points and `2500ms` adapter deadline. E0/E1 show strong rerank quality but also show that the current SiliconFlow 8B path misses the frozen latency/reliability gates even with a `5000ms` qualification deadline. M1 adds only a qualification-only `Qwen/Qwen3-Reranker-0.6B` alternate-model seam. M2 shows that 0.6B removes the latency/reliability blocker while retaining almost all aggregate quality; M3 attributes much of its strict failure to adversarial/equivalent-evidence effects while preserving one real partial-evidence loss. M4-M6 then froze and implemented a prospective disjoint-holdout v2 contract/execution path. M7 independently reproduced strong 0.6B quality and latency/reliability on H256 but missed only the frozen S16 top3-set stability gate at `15/16`; Owner accepts that known nondeterminism for the C1 explicit-search rerank line. C0 remains unchanged; C1-B real-host qualification is the next stage and remains separately unauthorized. Historical Q1/Q2 baselines remain frozen and are not retroactively rewritten.
4. **R4 Orchestration extraction — `POST-Q3 / BEHAVIOR-PRESERVING`.** Mechanically split the oversized `hybridSearch`, `createMemoryEngineExecute`, and `createAutoRecallHookLifecycle` orchestrators only after the retrieval decision is stable. No rewrite, new policy, or ranking change is implied.
5. **R5 AutoRecall activation blockers — `BEFORE SHADOW/CANARY`.** Broader AutoRecall enablement remains blocked until the deterministic intent gate is either removed/bypassed/requalified and recalled raw text has an explicit untrusted-data/instruction-isolation boundary. Prefer the existing card-first projection direction over adding more raw-text regex policy.
6. **R6 Governance slimming — `POST-Q2/Q3`.** Stop adding permanent audit/readiness/evidence modules for ordinary Level A/B work. Preserve `npm run review`, test-integrity, DB isolation, and Owner authorization for genuinely high-risk runtime/data changes, but move closed historical qualification machinery out of the production retrieval namespace or retire it when Git history/frozen evidence is sufficient. This is a maintainability cleanup, not another gated product stage.

**Small Q3-entry hygiene from the 2026-09-08 QA0 re-review — `SOURCE FIXED / FOCUSED VERIFIED / CLOSED`.** Dedicated `memory_engine_search` now exposes exact `memory_id` as primary `id` and does not fall back to a bounded compatibility alias when exact identity is absent; legacy `memory_engine action=search` keeps its bounded compatibility ID contract. `schemas.sql` now names the Engine-owned `~/.openclaw/memory/memory-engine/memory-engine.sqlite` and explicitly warns against applying the schema to Core `main.sqlite`. The adjacent LoCoMo scorer self-hash defect is also closed: finalized report bytes are written once and their SHA-256 is emitted to an external `.sha256` sidecar, with no recursive self-hash. Focused/affected tests are `44/44` PASS; static check `743` files PASS; test-integrity `338/0`; OpenSpec strict `12/12`; `git diff --check` clean. These are Level A source fixes only: no benchmark/provider rerun, runtime qualification, deployment, Gateway/config/DB/LanceDB mutation or AutoRecall change. Add-result `success=true` with `derived_state=pending|partial` remains the existing canonical-persistence/derived-readiness contract rather than a false-success bug. `agentAllowlist=["edi"]`, default `legacy_fallback`, broad `user_identity` routing, hit-driven tau inflation, and R2 TOCTOU remain non-Q3-blocking hygiene/backlog items.

**Recall-quality execution sequence:** Q0 `PASS_WITH_FINDINGS / CLOSED` → Q1 metric contract `CLOSED` and current baseline `FROZEN` → Q2-A1 channel harness `CLOSED` → Q2-A2 non-vector ablation `CLOSED` → Q2-B1 semantic binding `PASS / CLOSED` → **Q2-B2 always-vector execution `PASS_WITH_FINDINGS / CLOSED`** → **LME→LoCoMo fixed-candidate rerank confirmation `PASS_WITH_FINDINGS / QUALITY ACCEPTED_WITH_LIMITATIONS / CLOSED`** → **Q3 architecture direction `ACCEPTED_WITH_LIMITATIONS / OFFLINE SOURCE CLOSED / CHUNK QUALITY ACCEPTED_WITH_LIMITATIONS`**. Production rerank enablement, candidate depth/deadline, vector policy, set-aware selection and valid-topK/adaptive serving remain separate decisions. Q3 now retains the frozen evidence without authorizing another benchmark or retrieval-profile change.

**LME→LoCoMo fixed-candidate rerank confirmation — `PASS_WITH_FINDINGS / QUALITY ACCEPTED_WITH_LIMITATIONS / CLOSED`.** The LoCoMo durable runner at source `df602ca0d359cf6abef9f8ce92d6afd66f23aa31` completed pre-sentinel `16/16`, main `1,972/1,972`, and post-sentinel `16/16`: `2,004` valid new responses, `0` failed, `0` unknown, cumulative experiment request consumption `2,523`. Using the frozen semantic candidate identities and frozen Q1 @3 scorer, LoCoMo rerank reaches Recall-any@3 `0.917343`, Recall-all@3 `0.817951`, NDCG@3 `0.846151`, evidence coverage@3 `0.864477`; versus semantic ordering the deltas are `+0.335700 / +0.314909 / +0.360978 / +0.327712`. Paired transitions versus semantic are Recall-any `689/27/1256`, Recall-all `642/21/1309`, NDCG `1029/71/872`, evidence coverage `764/35/1173` (improved/regressed/unchanged). Multi-hop remains weakest but improves Recall-all@3 `0.054152 → 0.292419` and evidence coverage `0.248069 → 0.569725`. Feasible Recall-all@3 is `0.842298`; cross-session coverage@3 is `0.556149`. The preceding LongMemEval run shows the same direction and large magnitude: rerank `0.945107 / 0.754177 / 0.857112 / 0.851114`, deltas vs semantic `+0.202864 / +0.324583 / +0.294566 / +0.271997`. Cross-dataset generalization of fixed-candidate rerank headroom is therefore accepted. This does **not** prove that all gains come solely from the model, that semantic candidate generation is optimal, that always-vector is required, or that this provider/model should be deployed.

**Provider-stability finding.** LoCoMo post-sentinel completed with `2/16` exact score fingerprints unchanged and `14/16` changed; `5/16` had some ordering change, but `0/16` changed the top-3 set; maximum absolute score delta was `0.009059906`. Because the provider exposes no revision pin, raw-score reproducibility remains unqualified. The score artifact itself remains labeled `PROVISIONAL_POST_SENTINEL_COMPLETE`; Planner adjudication closes the quality/generalization question as accepted-with-limitations without rewriting that artifact field.

This roadmap update authorizes documentation planning only. It does not authorize provider requests, private production reads, runtime telemetry creation, AutoRecall enablement, or any Gateway/config/DB/LanceDB/live-plugin mutation. `AutoRecall=false` and the deployed production `topK=3` baseline remain unchanged.

With L2 Database Boundary Closure complete, **Phase 2.5-A is `PASS / CLOSED`**, **Phase 2.5-B is `PASS / CLOSED`**, and **Phase 2.5-C is `PASS_WITH_FINDINGS / CLOSED`** under `canonical-memory-architecture`. Phase 2.5-D source, deployment, and runtime qualification are **`PASS / QUALIFIED`**. Phase 2.5-D.1 source and deployment are **`PASS / CLOSED`**, and its live writer qualification is **`PASS / QUALIFIED`**. Overall Phase 2.5 Canonical Memory Architecture is **`PASS / CLOSED`**.

The memory-engine **`1.0.0` L2 stable runtime baseline is now `PASS / RUNTIME QUALIFIED / CLOSED`** on the accepted OpenClaw `2026.7.1-2` patched host runtime (`ded67f3cd7fde6ae19798a402c178e2d76fc2c71`). The deployed plugin source is `d201c8e5246773ba5e26dc8a959fa93beec9661d`; Gateway is `READY`; direct Gateway/operator explicit get remains denied, real WebChat Owner explicit get reaches the ordinary executor, AutoRecall/card-first remain disabled, and active disclosure attestations remain `0`. The post-deployment cleanup moved historical 0.8.22 backup directories out of plugin discovery and removed the duplicate-plugin warning without config or Engine DB drift. This runtime closeout does not expand the 1.0 product boundary or authorize OpenSpec 4.4.

**memory-engine Benchmark v1 B3 is `PASS_WITH_FINDINGS / OFFLINE BASELINE RECORDED`**. The official cleaned LongMemEval-S lexical profile (`production_hybrid_lexical_session_v1`, `top_k=50`, dataset SHA-256 `d6f21ea9d60a0d56f34a05b609c79c88a451d2ae03597821ea3d5a9678c3a442`) scores 419 cases after upstream-compatible exclusions. Overall recall-any is `0.5107/0.7876/0.9045/0.9952` at `@1/@5/@10/@50`; recall-all is `0.1289/0.4821/0.6372/0.9761`; NDCG-any@10 is `0.6256`; mean search latency is `9.49 ms`. First-loss fixes now accept official empty-string turns, preserve duplicate session-id corpus occurrences, and exclude non-abstention cases with no user-side target exactly as the upstream retrieval aggregate does. This is lexical/fusion evidence only. **B4 is `PASS_WITH_FINDINGS / BASELINE FROZEN / CLOSED`**: B4-S2 execution passed and the offline semantic baseline is recorded under `production_hybrid_semantic_session_v1` at source commit `b640c3547622a646d8962e27b20f0d5ac5a13cc1`, with Recall-any@5 `0.8138`, Recall-all@5 `0.5227`, Recall-all@10 `0.6826`, NDCG-any@10 `0.6552`, and mean retrieval latency `205.11 ms`. Its interpretation is `USEFUL BUT INSUFFICIENT`; this remains benchmark evidence, not production/runtime qualification or tuning authority. B5-I1 LoCoMo dataset/metric contract is `REPO-TESTED / CONTRACT FROZEN`, B5-I2/I2a/I2b hardening is `REPO-TESTED` under the historical profile `production_hybrid_lexical_dialog_locomo_v1`; the historical B5-S1 record is `PASS_WITH_FINDINGS / BASELINE FROZEN / CLOSED` with `TIME PROVENANCE INCOMPLETE` and is not strict semantic A/B authority. Current B5-S1-v2 is `PASS_WITH_FINDINGS / BASELINE FROZEN / CLOSED` under the time-frozen profile `production_hybrid_lexical_dialog_locomo_time_frozen_v2`; B5-I3 semantic v2 source implementation is `REPO-TESTED`; B5-S3 semantic v2 is `PASS_WITH_FINDINGS / OFFLINE BASELINE RECORDED / BASELINE FROZEN / CLOSED`; B5 overall is `PASS_WITH_FINDINGS / CROSS-DATASET GENERALIZATION RECORDED / CLOSED`. B6-A1 is `PASS_WITH_FINDINGS / SOURCE INSPECTION COMPLETE`; B6-I1 is `CLOSED`; B6-I2a is `CLOSED`; B6-I2 is `SOURCE IMPLEMENTATION REPO-TESTED / CLOSED` after retained-root restart/resume and cross-store crash reconciliation; B6-I2b is `SOURCE IMPLEMENTATION REPO-TESTED`; B6-I3-R2 is `SOURCE IMPLEMENTATION REPO-TESTED / DOCKER BUILD QUALIFIED`; B6-I3-R3 is `SOURCE IMPLEMENTATION REPO-TESTED / DOCKER RUNTIME CLOSURE QUALIFIED`; B6-I3 is `CLOSED`; B6-S1 is `PASS / LOCAL SYNTHETIC QUALIFICATION CLOSED / NO REAL PROVIDER`; official AML smoke/full remain unauthorized. Benchmark work does not authorize live runtime/config/DB mutation.

The current Benchmark v1 B6 route is now **B6-I1 `CLOSED`; B6-I2a `CLOSED`; B6-I2b `SOURCE IMPLEMENTATION REPO-TESTED / CLOSED`; B6-I2 `CLOSED`; B6-I3-R2 `PASS / DOCKER NATIVE DEPENDENCY PACKAGING FIXED`; B6-I3-R3 `SOURCE IMPLEMENTATION REPO-TESTED / DOCKER RUNTIME CLOSURE QUALIFIED`; B6-I3 `CLOSED`; B6-S1 `PASS / LOCAL SYNTHETIC QUALIFICATION CLOSED / NO REAL PROVIDER`; B6-S2 official AML smoke `NOT AUTHORIZED`; B6-S3 full `NOT AUTHORIZED`**. B6-S1 proves the local HTTP/Docker container, idempotency, isolation, graceful shutdown, retained-volume restart, and persistent embedding-cache path with deterministic fake embeddings only; it does not authorize a public service, real provider request, or official AML evaluation.

B6-I3-R1 is **`SEARCH ITEM DISCLOSURE/OUTPUT BOUNDARY HARDENED / REPO-TESTED`**. Search output now has an exact item allowlist, rejects unknown enumerable fields before HTTP 200, and requires finite numeric `score` and `created_at` values; B6-I3 remains closed at source level. This correction does not authorize B6-S1, provider requests, public deployment, or runtime data-plane operations.

B6-I3-R2 records the bounded Docker packaging correction after the B6-S1
Phase A `PASS` and initial Phase B `FAIL_WITH_FINDINGS`: the frozen
`node:24-bookworm-slim` dependencies stage could not build
`better-sqlite3@11.10.0` on Node 24 because Python and native build tools were
missing. The dependencies stage now installs `python3`, `make`, and `g++` with
`--no-install-recommends`, cleans `/var/lib/apt/lists/*`, and runs
`npm ci --omit=dev`; the independent runtime stage remains `node:24-bookworm-slim` and
only copies the produced `node_modules`. The Docker contract and requested
Node 24 checks pass, the real image build and native runtime loads pass, the
default UID is non-root (`1000`), and the final image contains none of the
builder toolchain or apt cache. B6-I3-R2 is **`SOURCE IMPLEMENTATION
REPO-TESTED / DOCKER BUILD QUALIFIED`**; B6-I3 is **`CLOSED`**; B6-S1 is
**`RETRY READY / PHASE B→D REMAINING`**, not closed.

B6-I3-R3 records the bounded Docker runtime file-closure correction after
B6-I3-R2 passed native dependency packaging and B6-S1 Phase B passed. The
initial B6-S1 Phase C attempt failed with `ERR_MODULE_NOT_FOUND` because the
runtime image omitted root `query-utils.js`, which is imported by
`lib/recall/hybrid-search.js`. The runtime stage now explicitly copies only
`query-utils.js` in addition to the frozen minimal surface: package manifests,
`bin/`, `lib/`, and builder-produced `node_modules`; `COPY . .` remains
forbidden. R3 source and rebuilt-image checks pass, including both service and
native module imports. At that historical checkpoint B6-S1 remained **`RETRY
READY / PHASE C→D REMAINING`**; the later local qualification below supersedes
that retry-ready state.

B6-S1 local synthetic HTTP/Docker qualification is **`PASS / CLOSED`** at source
`b2063f4531a5b0136de33ffee7e31c5235017028`. The real image passed health and
auth boundaries, Add/Search, same-`request_id` idempotency, and unknown-user
no-allocation against a deterministic local 2,560-dimensional fake embedding
endpoint. The first container stopped with exit `0`; its named volume preserved
the manifest, per-user Core/Engine SQLite, embedding cache, and user manifest; a
fresh second container recovered the same memory without re-Add and without a
new provider call, then also stopped with exit `0`. Synthetic Docker artifacts
were cleaned up. Real provider, official AML, public deployment, live runtime/data,
Gateway/config/plugin, and tag/push operations were not performed. The next B6
decision is B6-S2 official AML smoke, which remains **`NOT AUTHORIZED`**; B6-S3
full evaluation also remains **`NOT AUTHORIZED`**.

Benchmark v1 RH1 query-side embedding instruction is
`INSUFFICIENT / OFFLINE EXPERIMENT RECORDED / CLOSED`. RH1-S2 execution passed
under the independent profile
`production_hybrid_semantic_query_instruction_session_v1` at source commit
`d2d2fbd7e8eba77cd416923f44815e1289d4d7df` and dataset SHA-256
`d6f21ea9d60a0d56f34a05b609c79c88a451d2ae03597821ea3d5a9678c3a442`. Its
fixed query instruction moved overall Recall-any@5 by `+0.0095`, below the
pre-frozen `+0.0100` threshold; it did not resolve preference or multi-session
all-evidence coverage. B4 remains frozen and is still the semantic baseline
authority. RH1 does not qualify production/runtime behavior, authorize
instruction-text or production tuning, or change B3/B4.

`H2 = INSUFFICIENT / FULL EVALUATION NOT COMPLETED / PLANNER CONTRACT NOT
DATASET-ROBUST / OFFLINE EXPERIMENT RECORDED / CLOSED`. H2-S1 is `PASS / REAL-
PROVIDER CHAIN QUALIFIED`; H2-S2 is `INTERRUPTED / INCOMPLETE`; H2-S2-R1 is
`FAIL_CLOSED / PLANNER_OUTPUT_CONTRACT_VIOLATION`; and the six H2-S2 numerical
gate conditions are `NOT EVALUABLE`. H2-S2-R2 is `DO NOT RUN / NOT AUTHORIZED`.
`B5-I1 LoCoMo dataset/metric contract = REPO-TESTED / CONTRACT FROZEN`; that
earlier H2 snapshot predates B5-S1 and is superseded by the current B5 record
below. Benchmark evidence is not production authority.

The current B5 checkpoint is **B5-S1-v2 = `PASS_WITH_FINDINGS / BASELINE
FROZEN / CLOSED`** under the time-frozen profile
`production_hybrid_lexical_dialog_locomo_time_frozen_v2`. Its strict
denominator is `1,972` scored / `14` skipped, its sensitivity/retrieval
denominator is `1,978` scored / `8` skipped, and its three equal benchmark,
materialization, and search clocks are `1705066861`. The output/log/RC
provenance is recorded in the benchmark document; artifacts remain temporary.
The historical B5-S1 v1 record and metrics remain preserved with `TIME
PROVENANCE INCOMPLETE` and are not strict semantic A/B authority. The frozen
semantic vector invariant is attempted/skipped/error `1,978/0/0`, with
LanceDB `lancedb_search` and `vector_in_fusion=true` required for every
attempt, host-manager fallback `0`, and no silent lexical-only degradation.
The six numerical gates and regression guards compare semantic v2 against
lexical v2 without threshold changes. B5-I3 semantic v2 source implementation
is **`REPO-TESTED`**, and B5-S3 execution is **`PASS`**. Semantic v2 quality is
**`PASS_WITH_FINDINGS`** with five of six gates passing and all regression guards
passing; Multi-hop Recall-all@10 is below threshold. B5 semantic v2 is
**`OFFLINE BASELINE RECORDED / BASELINE FROZEN / CLOSED`**, and B5 overall is
**`PASS_WITH_FINDINGS / CROSS-DATASET GENERALIZATION RECORDED / CLOSED`**.
Benchmark evidence is not production authority.

Intent-aware Recall v2-A is **`SOURCE IMPLEMENTED / VERIFIED`**. Its deterministic task/recall taxonomy is observational only: frozen 12-row replay now enforces both labels, and decision trace/debug metadata expose bounded intent values. Existing recall decisions, focused queries, retrieval policy, ranking, Card/Get behavior, and AutoRecall default-off state remain unchanged. Policy authority remains a separate decision.

Intent-aware Recall v2-B1 remains an **offline-only evaluation**. Its initial 36-row result was V1 `18/5/13/0`, oracle `18/18/0/0`, and runtime candidate `8/18/0/10` for TP/TN/FP/FN, with 4 task and 12 recall mismatches.

Intent-aware Recall v2-B2 is **`SOURCE IMPLEMENTED / KNOWN-GAP REGRESSION CLOSED`**. Generalized deterministic classifier signals now produce zero task/recall mismatch and runtime candidate `18/18/0/0` on the frozen B1 regression set; V1 remains unchanged at `18/5/13/0`. The candidate policy is **not runtime authorized**.

Intent-aware Recall v2-B3's first independent run is a **historical `PASS_WITH_FINDINGS / HOLDOUT NOT READY`** record: the valid 48-row, 24/24 Planner-frozen holdout produced oracle `24/24/0/0` and runtime `7/19/5/17`, with precision `0.5833`, recall `0.2917`, task mismatches `14`, and recall mismatches `25`. B4 does not rewrite that first-run result.

Intent-aware Recall v2-B4 is **`SOURCE IMPLEMENTED / STRUCTURED EVIDENCE REGRESSION CLOSED`**. The classifier now uses pure request-scope and structured history evidence with quoted-content masking and suppression precedence. The frozen v2-A seed remains `12/12`; B1 remains V1 `18/5/13/0` and runtime candidate `18/18/0/0`; B3 current regression is V1 `24/0/24/0` and runtime candidate `24/24/0/0`, with task/recall mismatch `0/0`. B3 is now explicitly **`REGRESSION ONLY / NOT INDEPENDENT READINESS EVIDENCE`** with evidence role `known_regression_after_v2b4`; these scores do not qualify generalization or authorize the candidate policy.

Retrieval-first Selective Disclosure Phase D.2-B is **`FAILED / PRODUCT ARCHITECTURE GAP`**. The frozen v2 candidate-level evaluation produced `41` selected cards and `7` withheld candidates, with `23` unsafe disclosures, `0` unauthorized full-content surfaces, and answer-bearing disclosure recall `0.75`. This is an offline product finding, not a fixture or runtime failure; no selector, admissibility, evaluator, fixture, or runtime source was changed.

The **Disclosure Capability Contract v1.1** is **`PASS / CONTRACT CLOSED`**. It separates retrieval availability, internal context, bounded card disclosure, and reserved raw disclosure; `safe_to_disclose` is owned by capability calculation and is required for `CARD_DISCLOSABLE`. The contract does not itself authorize production integration or runtime adoption.

The **Disclosure Capability Shadow Evaluation v1.1** is **`PASS / OFFLINE EVALUATION COMPLETE`**. The pure offline evaluator is implemented at commit `8db96038cb44818df2674b5f3b87a99adab93728` and preserves the existing production selector. On the frozen 48-row v2 fixture (`d372ebac9bf4d80aaac85dfb0d43eacec792291871b744b7812009ac06d071bd`), D.2-C.11 reduced unsafe card disclosure from `23` to `0`, while answer-bearing disclosure recall remained `0.75`; selected cards changed `41 -> 18` and irrelevant disclosures `23 -> 0`. This is offline architecture evidence only: no production selector/admissibility change, runtime integration, deployment, AutoRecall enablement, config mutation, or data mutation occurred.

**Phase D.3 Memory Projection Architecture** has reached its 1.0 production closeout on the DIRECT_CARD branch; D.3-D.4 / OpenSpec 4.3 is `PASS / RUNTIME QUALIFIED / CLOSED`, while OpenSpec 4.4 remains unchecked and `REDACTED_CARD` / `INTERNAL_AGENT_CONTEXT` remain non-production research directions. D.3-A closed the architecture contract, D.3-B is `SOURCE IMPLEMENTED / VERIFIED`, D.3-C.1 is **`HOLDOUT CONTRACT FROZEN`**, D.3-C.2 is **`SOURCE IMPLEMENTED`**, D.3-C.3 first-run evidence is **`ACCEPTED BY PLANNER FOR ARCHITECTURE INTERPRETATION`**, D.3-C.4 is **`PROJECTION STRATEGY TAXONOMY DEFINED`**, D.3-C.5 is **`REDACTED_CARD OFFLINE PROTOTYPE IMPLEMENTED / NOT INDEPENDENTLY EVALUATED`**, D.3-C.6 is **`INDEPENDENT REDACTED_CARD HOLDOUT FROZEN / NOT YET EVALUATED`**, D.3-C.7 is **`PURE INDEPENDENT REDACTED_CARD EVALUATOR IMPLEMENTED`**, D.3-C.8 is **`PASS / FIRST-RUN EVIDENCE ACCEPTED`**, D.3-C.9 is **`REDACTION PLAN AUTHORITY BOUNDARY DEFINED`**, D.3-C.10 is **`STRUCTURED REDACTION EVIDENCE CONTRACT IMPLEMENTED / OFFLINE ONLY / NOT PRODUCTION AUTHORITY`**, D.3-C.11 is **`REDACTION EVIDENCE RESOLUTION SEMANTICS DEFINED`**, D.3-C.12 is **`INTERNAL_AGENT_CONTEXT REPRESENTATION BOUNDARY DEFINED`**, D.3-C.13 is **`INTERNAL_AGENT_CONTEXT PROJECTION CONTRACT PROTOTYPE IMPLEMENTED / OFFLINE ONLY / NOT CAPABILITY AUTHORIZED`**, D.3-C.14 is **`INDEPENDENT INTERNAL_AGENT_CONTEXT HOLDOUT FROZEN`**, D.3-C.15 is **`PURE INDEPENDENT INTERNAL_AGENT_CONTEXT EVALUATOR IMPLEMENTED`**, and D.3-C.16 is now **`PASS / FIRST-RUN EVIDENCE ACCEPTED`**. D.3-C product interpretation is **`ACCEPTED`** and D.3-D entry is **`APPROVED FOR DIRECT_CARD SOURCE MIGRATION ONLY`**. D.3-D.1 design was **`PASS_WITH_FINDINGS`**; at that historical decision point, direct-card source migration was **`HOLD`** with historical blocker **`PRODUCTION_SAFE_TO_DISCLOSE_AUTHORITY_MISSING`**. D.3-D.2 authority decision was **`PASS / DECISION CLOSED`**; its historical decision-point blocker was **`PRODUCTION_DISCLOSURE_ATTESTATION_PROVIDER_NOT_IMPLEMENTED`**. D.3-D.3 source implementation is **`IMPLEMENTED / REPOSITORY-TESTED`** with no AutoRecall wiring or runtime deployment; At that historical pre-H2-E snapshot, D.3-D.4 was **`AUTHORIZED / PRE-IMPLEMENTATION BLOCKED`** and **`BLOCKED / NOT IMPLEMENTED`** for source migration because the trusted same-run `OWNER_SELF` audience proof was unavailable at the prompt-injection boundary. Blocker: **`PRODUCTION_OWNER_SELF_AUDIENCE_AUTHORITY_UNAVAILABLE_AT_PROMPT_INJECTION_BOUNDARY`**. The full matrix and D.3-D.1 boundary record are in `docs/memory-projection-product-interpretation-v1.md` and `docs/direct-card-production-boundary-migration-design-v1.md`; no C.17 was created, and no production or runtime mutation is authorized.

The preceding D.3-D paragraph is a historical pre-H2-E status snapshot. The
current consumer status is the H2-E `PASS / SOURCE IMPLEMENTED /
REPOSITORY-TESTED` record below, followed by the separately authorized runtime
qualification closeout.

The current D.3-D.4 / OpenSpec 4.3 roadmap state is **`PASS / RUNTIME
QUALIFIED / CLOSED`**, recorded in `docs/direct-card-runtime-qualification-v1.md`.
The DIRECT_CARD qualification baseline remains conservative: AutoRecall and card-first runtime are disabled and active attestations were `0` at the 4.3 closeout. The original qualified H2 deployed source was `755ff5d396102c5a93baacf2d3bf6187a4fea713`; the current active OpenClaw build is the later authority-domain corrective `ba0edf98d1617b92296e7b834ee7a5a82cc2a18e`, which preserves the closed DIRECT_CARD contract while separating Gateway/core Owner semantics from plugin conversational Owner authority. OpenSpec 4.4 remains unchecked and raw disclosure remains out of scope.

The D.3-D.2 `PRODUCTION_DISCLOSURE_ATTESTATION_PROVIDER_NOT_IMPLEMENTED`
wording above records the historical blocker at that decision point. D.3-D.3
subsequently implemented the repository source provider and Owner boundary.
The statement that the provider was not wired into production AutoRecall and
that source migration remained `HOLD` is a historical pre-H2-E status snapshot.
H2-E later supplied the consumer source path, and the 2026-08-24 runtime
qualification superseded that source-stage state: D.3-D.4 / OpenSpec 4.3 is
now **`PASS / RUNTIME QUALIFIED / CLOSED`**. The D.3-D.4 pre-implementation
review remains a historical record and is not pending.

The D.3-D.3 status includes same-stage corrective closure of the obsolete
two-argument command registration and the placeholder Owner preview. The
corrected preview is Canonical-source-derived and binds the independent
`owner_attestable_canonical_card_v1` adapter.

D.3-D.4-H1 **`PASS_WITH_FINDINGS / CONTRACT DESIGN FROZEN`** records the
selected event-local, same-run, host-authenticated `before_prompt_build`
`senderIsOwner?: boolean` contract. The installed OpenClaw `2026.6.9` package
was inspected for design evidence, but no authoritative upstream checkout was
available locally; finding: **`OPENCLAW_AUTHORITATIVE_SOURCE_CHECKOUT_NOT_PRESENT_LOCALLY`**.
At the H1 design-freeze point, no OpenClaw or memory-engine source
implementation was authorized; H2 was **`CANDIDATE / NOT AUTHORIZED`** and
D.3-D.4 was blocked on the audience boundary. Later H2, H2-E, and the
2026-08-24 runtime qualification superseded that design-freeze state; the
current D.3-D.4 / OpenSpec 4.3 state is **`PASS / RUNTIME QUALIFIED /
CLOSED`**.

The later authorized D.3-D.4-H2-E consumer stage is **`PASS / SOURCE
IMPLEMENTED / REPOSITORY-TESTED`**. It consumes the verified host event-local
`senderIsOwner?: boolean` contract from OpenClaw H2 source commit
`2e67ab06b6f7f1cf7655d0a8d508fc2a2ad78176`, with deployed correction
`755ff5d396102c5a93baacf2d3bf6187a4fea713`, migrates the enabled DIRECT_CARD
capability/selector/card path, and derives telemetry from selected cards only.
The H1 source-checkout finding is retained as historical; the authoritative
source correction is that `v2026.6.9` contains the CLI prompt-build path and H2
covers it. Runtime qualification is now separately closed by the record cited
above.

The former non-blocking explicit-tool follow-up is now closed: `EXPLICIT_MEMORY_TOOL_DISCLOSURE_BOUNDARY_NOT_UNIFIED = CLOSED / RUNTIME QUALIFIED / PASS` under active OpenClaw `ba0edf98d1617b92296e7b834ee7a5a82cc2a18e` and memory-engine `cd80a25f089a7eb7b5e0d89c476fb4dc86b0e4cb`. `memory_engine_search` is bounded untrusted retrieval; `memory_engine_get` is Owner-only explicit full-content retrieval; and Gateway/operator Owner semantics are separated from conversational plugin Owner authority. Evidence: `docs/explicit-memory-tool-runtime-qualification-v1.md`.

Other retained follow-ups outside 4.3 include the already closed external `null` confidence coercion finding (`EXTERNAL_NULL_CONFIDENCE_COERCED_TO_ZERO`) and the resolved EDi agent-ID baseline correction from `edi` to `main` (`AUTO_RECALL_AGENT_ID_DEFAULT_MISMATCH`). None creates a new stage or authorizes 4.4.

### NEXT

#### memory-engine 1.1 Q2-B2 → Q3

**1.1-Q1 Product Metric Contract is `CLOSED`; the current lexical baseline is `FROZEN`.** The accepted small-budget authority includes Recall-any@3, Recall-all@3, NDCG@3, first relevant rank, evidence coverage@3 and feasibility-aware full recall, with dataset/category breakdowns and bounded latency accounting where authority exists. Recall@50 remains diagnostic. Gold evidence stays evaluator-only and must not enter Add, Search, embedding, query planning, ranking, or answer prompts.

**1.1-Q2-B2 is accepted and closed with findings.** Q2-A1 channel-ablation harness is `CLOSED`; Q2-A2 non-vector ablation is `CLOSED` and found zero Q1-quality deltas because the current benchmark materialization does not activate KG/Recent candidates; Q2-B1 always-vector semantic binding is `PASS / CLOSED`. **Q2-B2 Always-Vector Semantic Execution is `PASS_WITH_FINDINGS / CLOSED`.** Retrieval commit `36b480be4db396f83273e408c0a3d4d17a1635cf` and corrected binding commit `805ff9307757b489dd262c6fb59e786c73967354` are recorded separately. LongMemEval any/all@3 gains are +0.033413/+0.045346; LoCoMo gains are +0.099391/+0.091278. Paired comparisons cover 419/1972 cases. LongMemEval multi-session regresses, and LoCoMo multi-hop full-recall improvement remains small. Failed-attempt total cost is unknown; semantic top50 latency is not comparable to Q1 top3 latency. See `docs/memory-engine-q2-semantic-binding-v1.md` for adjudication. Selective vector remains `NOT IMPLEMENTED` and is an undecided candidate-generation policy; Q3 did not select it. Do not mix R3 canonical valid-topK serving changes, new ranking formulas, unconditional multi-query, or broad model grids into this frozen comparison.

**1.1-Q3 architecture direction is `ACCEPTED_WITH_LIMITATIONS / OFFLINE SOURCE CLOSED / CHUNK QUALITY ACCEPTED_WITH_LIMITATIONS`; R3 explicit-search serving is `PASS_WITH_FINDINGS / SOURCE CLOSED`; and R3-C0 control runtime qualification is `CLOSED`.** Fixed-candidate evidence supports retaining a true relevance-rerank layer and the longer-term working direction `candidate generation → bounded pre-rank/fusion → true relevance rerank → set-aware evidence selection → canonical validation → adaptive 0..K serving`. Candidate generation remains a separate lexical/metadata/vector-policy decision; the FTS-only chunk experiment does not establish always-vector or selective-vector superiority. The accepted R3 explicit-search profile freezes bounded valid-pool serving, and C0 qualifies its control path at production-shaped inputs (`topK=3`, depth `20`, `4000`/`48000` code-point budgets, `2500ms` deadline). C1 provider selection, default provider enablement, set-aware selection, adaptive cutoff, always-vector/selective-vector policy, and real canonical-text egress remain separate decisions. Historical Q1/Q2 baselines remain frozen.

**Cross-dataset rerank interpretation.** The LME and LoCoMo runs held candidate identity fixed and changed the ordering with the same frozen rerank configuration, yet both produced large @3 gains. That is stronger direct evidence for a missing true relevance-rerank layer than the earlier score-scale diagnosis alone. It does not remove the fusion problem: current `scoreCandidate()` still mixes heterogeneous channel `similarity`, RRF, and metadata boosts on incomparable scales. Fusion calibration is therefore retained as a cheaper pre-rank/fallback experiment and a possible way to reduce rerank depth/cost, rather than being treated as the sole explanation for rank loss.

**Q3 fusion/ranking diagnosis — accepted as a testable architecture hypothesis, not as a causal conclusion.** Current `scoreCandidate()` directly adds `semanticScore + rrfScore + categoryBoost + recencyBoost + confidenceBoost + externalBoost`. The scale mismatch is real: with `rrfK=60`, one channel's rank-1 contribution is about `0.0164`, four rank-1 channel hits total about `0.0656`, and rank-1 versus rank-10 in one channel differs by only about `0.0021`, while configured metadata terms can contribute roughly `0.05–0.12`. More fundamentally, current `semanticScore` is not a pure vector-cosine feature: normalized candidates copy each channel's heterogeneous `similarity` into `semantic_score`, and fusion keeps the maximum. FTS, KG, Recent, and Vector therefore contribute differently constructed score scales before this additive blend. This justifies a ranking-only calibration experiment; it does **not** prove that the fusion formula caused all observed rank failures.

**RANK_MISS interpretation is narrowed.** The frozen LongMemEval Q0 source has `248/419` scored cases classified as `RANK_MISS`, meaning the complete gold evidence set is present within the bounded top-50 candidate result but not fully inside production top-3 under the frozen adapter. This is not equivalent to "59.2% are caused by four-channel fusion". Within the frozen 248-case rank-miss source, `217` cases require at most three evidence sessions and are therefore structurally capable of full recall at top-3; `31` require more than three and cannot achieve full recall under a strict top-3 budget regardless of reranker quality. Q3 should optimize the feasible ranking/selection loss separately from budget-infeasible evidence-set cases.

**Q3 architecture decision — 2026-09-12:**

The independent relevance-rerank interface, canonical text projection and orchestrator are retained as the integration foundation. On the same frozen FTS-only chunk candidates, Recall-all@3 improved from `45.08%` to `68.73%`, with 476 improved and 10 regressed cases. Provider error, timeout, invalid response or persistence failure remains an all-or-nothing fallback to the same-profile control order. The acceptance report is [memory-engine-q3-locomo-chunk-rerank-acceptance-v1.md](memory-engine-q3-locomo-chunk-rerank-acceptance-v1.md).

The original architecture decision accepted the rerank direction without production parameters. Subsequent bounded source work closed R3 explicit-search serving, and R3-C0 qualified the control path at depth `20`, `4000` code points per candidate, `48000` total code points and `2500ms` adapter deadline for live product `topK=3`. These are qualified control-profile bounds, not evidence that depth 20 is quality-optimal and not authorization for a real provider. R3-C1-A-I0 subsequently source-closed qualification tooling for SiliconFlow `Qwen/Qwen3-Reranker-8B` while preserving the frozen FTS-only candidate-generation limitation; this selects a qualification candidate, not a production provider default. Real provider execution, provider/account budgets, canonical-text egress, set-aware evidence selection, adaptive `0..K`, always-vector/selective-vector policy and provider runtime activation remain separate decisions. The experiment's depth `50` and recovered `10s` deadline remain historical experiment parameters, not production defaults.

The completed runner's stale `recovery.status` is an ordinary adjacent source defect. The experiment is reconciled as complete; this finding does not block the Q3 architecture decision and must not be repaired by editing historical experiment state.

**Historical R3 qualification record — superseded by the closed C0 runtime evidence above.** The earlier disabled real-host compatibility was `PASS / CLOSED` for the `4bff77a` artifact, and the first C0 control-only qualification stopped at preflight with no live mutation or execution-count consumption. That record is retained for provenance; it is not a pending deployment or retry. The final `5f263b3` candidate/runtime closure supersedes it. Production remains C0-absent (`explicitSearchRerankControl` absent); the later C1-A-I0 source closure does not change production and does not authorize provider use. No new provider run is implied.
6. **Candidate-first AutoRecall remains later, before shadow/canary.** The targeted synthetic trigger recall `0.125` is evaluation evidence, not production prevalence, but it is sufficient to keep deterministic intent authority from regaining production control without requalification. The preferred later direction is cheap candidate retrieval first, then candidate-quality/injection decision, retaining only minimal structural skips where justified. This does not authorize AutoRecall activation.

Query expansion, temporal/entity/coreference understanding, and Recall Hint remain primarily **Q4** work. The product code already carries bounded multi-query vector plumbing, but historical H2 is `INSUFFICIENT / CLOSED` because its planner contract was not dataset-robust; that implementation is not simply promoted to production as part of Q3.

The D.3-D.4 source-migration clauses in the historical sequence below describe the pre-H2-E decision point. The current DIRECT_CARD source status is H2-E `PASS / SOURCE IMPLEMENTED / REPOSITORY-TESTED`, and its separate runtime qualification remains `PASS / RUNTIME QUALIFIED / CLOSED`. The explicit memory-tool authority follow-up is also `CLOSED / RUNTIME QUALIFIED / PASS`; no additional explicit-tool canary or source stage remains. No 4.4 work is authorized by either closeout.

The next compact D.3-D sequence paragraph retains the historical source-stage
statuses for auditability; its pre-runtime `HOLD` / unchecked wording is not
the current roadmap state.

Phase 2.5 Canonical Memory Architecture is closed. No further D/D.1 deployment or qualification work remains. Intent-aware Recall v2-B and the successor Selective Recall Gate deterministic-authority experiment are closed: the full semantic route and skip-only C1 route are rejected, and v2-B6/C2-C repair are cancelled. Retrieval-first Selective Disclosure D.2-C completed its capability contract and offline v1.1 shadow evaluation.

#### Benchmark v1 external-quality track

Benchmark v1 is a persistent **evaluation track**, not a new product phase and not a runtime/deployment authority. It provides independent version-comparable evidence for later retrieval, Recall Hint, LTR, AutoRecall, and multi-agent changes while preserving internal frozen fixtures as regression evidence only.

Accepted sequence:

1. **B1 — Dataset/metric contract: `PASS / CLOSED`**. LongMemEval schema normalization, neutral Add/Search envelopes, evaluator-only gold labels, and LongMemEval-compatible session metrics are established.
2. **B2 — Isolated production-retrieval adapter: `PASS / CLOSED`**. Benchmark-owned temporary Core/Engine/FTS state reuses production `hybridSearch()` without touching active OpenClaw/memory-engine state.
3. **B3 — LongMemEval-S lexical baseline: `PASS_WITH_FINDINGS / BASELINE FROZEN`**. `production_hybrid_lexical_session_v1` on cleaned LongMemEval-S is the immutable comparison point for later retrieval profiles. Do not tune lexical heuristics against this dataset before the semantic/vector comparison.
4. **B4 — Real semantic/vector profile: `PASS_WITH_FINDINGS / BASELINE FROZEN / CLOSED`**. The separately named profile uses memory-engine's real embedding/vector path while holding dataset, session granularity, scoring, lexical baseline, and reporting contract fixed. B4-S2 execution is `PASS`; the final quality adjudication is `PASS_WITH_FINDINGS`; the offline baseline is recorded and frozen. Primary comparison targets remain `Recall-any@5`, `Recall-all@5`, `Recall-all@10`, `NDCG-any@10`, per-family deltas, and retrieval latency. Semantic interpretation is `USEFUL BUT INSUFFICIENT`: it does not qualify production/runtime behavior, solve preference or multi-evidence coverage, or authorize production tuning. A B4 regression must not be hidden by replacing B3 results.
5. **H2 — Bounded multi-query semantic profile: `INSUFFICIENT / FULL EVALUATION NOT COMPLETED / PLANNER CONTRACT NOT DATASET-ROBUST / OFFLINE EXPERIMENT RECORDED / CLOSED`**. H2-S1 is `PASS / REAL-PROVIDER CHAIN QUALIFIED`, but H2-S2 was `INTERRUPTED / INCOMPLETE` and H2-S2-R1 was `FAIL_CLOSED / PLANNER_OUTPUT_CONTRACT_VIOLATION`. The real planner returned a query duplicating the exact production query on the official dataset, so the frozen strict planner contract was not dataset-robust. The six H2-S2 numerical gate conditions are `NOT EVALUABLE` because no complete output exists; no overall, family, or case-level H2 metrics may be inferred. H2-S2-R2 is `DO NOT RUN / NOT AUTHORIZED`; do not loosen the parser, retry, hand-fill a case, or reopen H2 in place. B4 remains the frozen semantic baseline authority, and this result is not production/runtime/deployment authority.
6. **B5 — Cross-dataset conversational generalization: `B5-I1/I2/I2a/I2b REPO-TESTED; B5-S1-v2 PASS_WITH_FINDINGS / BASELINE FROZEN / CLOSED; B5-I3 SOURCE IMPLEMENTATION REPO-TESTED; B5-S3 PASS; B5 OVERALL PASS_WITH_FINDINGS / CROSS-DATASET GENERALIZATION RECORDED / CLOSED`**. The LoCoMo dataset/metric contract and isolated lexical/semantic retrieval runners use pinned upstream/dataset provenance, strict and separately named canonicalized evidence policies, conversation-owned temporary Core/Engine/FTS/LanceDB corpora, dialog-level primary metrics, session projection compatibility metrics, the fixed `locomo_dialog_projection_v1` format with session date plus non-empty captions, and one resolved benchmark time for the complete dataset run. The historical B5-S1 v1 execution and metrics remain preserved with incomplete clock provenance and are not strict semantic A/B authority. The time-frozen lexical v2 baseline remains frozen as the semantic comparison authority. B5-S3 semantic v2 passed execution and recorded `PASS_WITH_FINDINGS`: five of six numerical gates passed, all regression guards passed, and Multi-hop Recall-all@10 remained below threshold; its mean retrieval latency was `364.761142 ms` versus lexical v2 `15.455937 ms` (`23.60×`). This is cross-dataset benchmark evidence, not production/runtime policy authority. At the B5 closeout checkpoint, B6-A1 source inspection was complete and B6-I1/I2 were closed; the current B6 state is recorded in item 7 below.
7. **B6 — External controlled comparison: `B6-A1 PASS_WITH_FINDINGS / SOURCE INSPECTION COMPLETE; B6-I1 CLOSED; B6-I2a CLOSED; B6-I2 SOURCE IMPLEMENTATION REPO-TESTED / CLOSED; B6-I2b SOURCE IMPLEMENTATION REPO-TESTED / CLOSED; B6-I3-R2 PASS / DOCKER NATIVE DEPENDENCY PACKAGING FIXED; B6-I3-R3 SOURCE IMPLEMENTATION REPO-TESTED / DOCKER RUNTIME CLOSURE QUALIFIED; B6-I3 CLOSED; B6-S1 PASS / LOCAL SYNTHETIC QUALIFICATION CLOSED / NO REAL PROVIDER; B6-S2 OFFICIAL SMOKE NOT AUTHORIZED; B6-S3 FULL NOT AUTHORIZED`**. The pinned AML Add/Search adapter uses per-`user_id` temporary Core/Engine/vector-path topology, persistent request-id idempotency, production `hybridSearch()` ranking, production confidence/vector gate defaults, exact-question Search with options excluded from retrieval input, host-manager fallback forbidden, and the current 240-character Hybrid evidence surface. B6-I1 is closed after repository-only implementation. B6-I2a adds an explicitly injected semantic provider boundary, the existing embedding-cache contract, real temporary per-user LanceDB storage, deterministic canonical-row reuse/repair, and operation-scoped cache/table/connection ownership. B6-I2b adds durable PENDING/READY ledger ordering, confidence-0 pending visibility, retained-root manifest/ownership validation, lazy existing-user reopen, exact Core/FTS and vector reconciliation, and child-process crash-boundary tests. B6-I3 adds the canonical Node HTTP transport, env-only semantic provider wiring, bounded/sanitized request handling, graceful retained-root shutdown, and Node 24 non-root Docker packaging; B6-I3-R2 confines native build tooling to the discarded dependencies stage and B6-I3-R3 confines runtime packaging to the explicit minimal file closure. No provider request, official AML smoke/full run, live runtime/data mutation, public deployment, or leaderboard upload is authorized by this status.
8. **B7 — Answer-quality layer: `LATER / SEPARATE CONTRACT`**. Only after retrieval profiles are stable, introduce a fixed answering-model/evaluator contract so QA quality is not conflated with retrieval quality. LongMemEval-M scale runs may be added here or after B4 as capacity/performance evidence, but they do not replace S as the frozen comparison baseline.

The current benchmark decision is **B6-I3-R3 `SOURCE IMPLEMENTATION REPO-TESTED /
DOCKER RUNTIME CLOSURE QUALIFIED`; B6-I3 `CLOSED`; B6-S1 `PASS / LOCAL SYNTHETIC
QUALIFICATION CLOSED / NO REAL PROVIDER`** after I2a semantic backend/resource
ownership, I2b retained-root restart/reconciliation, the benchmark-local
HTTP/Docker transport, the bounded R2/R3 Docker packaging corrections, and the
successful local container/restart qualification.
B6-S2 official AML smoke and B6-S3 full evaluation are `NOT AUTHORIZED`.
Historical H2 remains
`INSUFFICIENT / CLOSED`; its incomplete/full-evaluation findings are not reopened
or repaired by B6.

Benchmark evidence is diagnostic, not self-authorizing. A poor family score may justify a bounded product investigation, but fixture/benchmark tuning must not silently create production heuristics. The current benchmark route is B6-I3-R3 `SOURCE IMPLEMENTATION REPO-TESTED / DOCKER RUNTIME CLOSURE QUALIFIED`; B6-I3 is `CLOSED`; B6-S1 is `PASS / LOCAL SYNTHETIC QUALIFICATION CLOSED / NO REAL PROVIDER`, while official AML smoke/full remain unauthorized. Historical H2 stays `INSUFFICIENT / CLOSED` and is not reopened. Candidate directions beyond the current bounded multi-query source work remain candidates only: query decomposition, temporal query expansion, Recall Hint, entity expansion, and iterative/multi-hop retrieval. Do not directly start LTR or modify production lexical heuristics from LongMemEval results. If a proposal is designed from B4 or H2 failures, subsequent LongMemEval results are development/regression evidence only; product policy requires LoCoMo or other cross-dataset evidence. Benchmark evidence is not production authority. D.3-A and D.3-B are closed at architecture/source level; D.3-C.1 is **`HOLDOUT CONTRACT FROZEN`**, D.3-C.2 is **`SOURCE IMPLEMENTED`**, D.3-C.3 evidence is **`ACCEPTED BY PLANNER FOR ARCHITECTURE INTERPRETATION`**, D.3-C.4 is **`PROJECTION STRATEGY TAXONOMY DEFINED`**, D.3-C.5 is **`REDACTED_CARD OFFLINE PROTOTYPE IMPLEMENTED / NOT INDEPENDENTLY EVALUATED`**, D.3-C.6 is **`INDEPENDENT REDACTED_CARD HOLDOUT FROZEN / NOT YET EVALUATED`**, D.3-C.7 is **`PURE INDEPENDENT REDACTED_CARD EVALUATOR IMPLEMENTED`**, D.3-C.8 is **`PASS / FIRST-RUN EVIDENCE ACCEPTED`**, D.3-C.9 is **`REDACTION PLAN AUTHORITY BOUNDARY DEFINED`**, D.3-C.10 is **`STRUCTURED REDACTION EVIDENCE CONTRACT IMPLEMENTED / OFFLINE ONLY / NOT PRODUCTION AUTHORITY`**, D.3-C.11 is **`REDACTION EVIDENCE RESOLUTION SEMANTICS DEFINED`**, D.3-C.12 is **`INTERNAL_AGENT_CONTEXT REPRESENTATION BOUNDARY DEFINED`**, D.3-C.13 is **`INTERNAL_AGENT_CONTEXT PROJECTION CONTRACT PROTOTYPE IMPLEMENTED / OFFLINE ONLY / NOT CAPABILITY AUTHORIZED`**, D.3-C.14 is **`INDEPENDENT INTERNAL_AGENT_CONTEXT HOLDOUT FROZEN`**, D.3-C.15 is **`PURE INDEPENDENT INTERNAL_AGENT_CONTEXT EVALUATOR IMPLEMENTED`**, and D.3-C.16 is now **`PASS / FIRST-RUN EVIDENCE ACCEPTED`**. D.3-C product interpretation is **`ACCEPTED`** and D.3-D entry is **`APPROVED FOR DIRECT_CARD SOURCE MIGRATION ONLY`**. D.3-D.1 design was **`PASS_WITH_FINDINGS`** with historical blocker **`PRODUCTION_SAFE_TO_DISCLOSE_AUTHORITY_MISSING`**. D.3-D.2 authority decision was **`PASS / DECISION CLOSED`** with historical blocker **`PRODUCTION_DISCLOSURE_ATTESTATION_PROVIDER_NOT_IMPLEMENTED`**; D.3-D.3 subsequently provided the repository source implementation and Owner management boundary, but it is not wired into production AutoRecall. Current direct-card source migration remains **`HOLD`** under the D.3-D.4 audience-boundary blocker, and the D.3-D.4 pre-implementation review is complete, not pending. The C.16 evidence, product decision, and D.3-D.1 design are in `reports/internal-agent-context-holdout-v1-first-run-20260823.md`, `docs/memory-projection-product-interpretation-v1.md`, and `docs/direct-card-production-boundary-migration-design-v1.md`; no C.17 was created. The D.2, D.3-C.1, C.6, and C.14 holdouts remain immutable. `ADD_SYNC_BACKFILL_SCOPE_FINDING` remains a non-blocking operational/lifecycle finding outside D.1 scope. D.3-D.3 source implementation is **`IMPLEMENTED / REPOSITORY-TESTED`** using temporary/in-memory test databases only; no runtime deployment or real attestation state was created. At that historical source-stage snapshot, D.3-D.4 was **`AUTHORIZED / PRE-IMPLEMENTATION BLOCKED`** and **`BLOCKED / NOT IMPLEMENTED`** for source migration because `PRODUCTION_OWNER_SELF_AUDIENCE_AUTHORITY_UNAVAILABLE_AT_PROMPT_INJECTION_BOUNDARY`; no D.3 runtime authority had yet been granted.

### CLOSED

- The persistent-activation qualification line is closed. The separately authorized Session-Flush Reconciliation Persistent Rollout Successor recorded `PASS` on 2026-08-17, accepted the immutable `ac0e5f0` reconciliation candidate for persistent use, and explicitly requires no R5 or additional persistent-activation qualification. AutoRecall remains disabled by default and broad rollout remains a separate later decision.
- **L2 Database Boundary Closure** recorded `deployed runtime qualification PASS / L2 COMPLETE` at active runtime source parity `057f43e`. Gateway was `READY`; `AutoRecall=false`; Hybrid runtime used `KG_ACCESS_MODE=isolated` and `RECENT_ACCESS_MODE=isolated` with production legacy fallback `0`; the current Nightly dry-run isolated topology passed; and no unexpected memory/confidence mutation was observed.
- **Conflict Ownership Closure** is `SOURCE CLOSED / PASS`: Session Checkpoint exclusively owns `category='preference'` conflict flags through `resolvePreferenceConflicts()` / `preference_latest_wins`, while generic lifecycle detection excludes preference and preserves non-preference conflict behavior. No schema change was made. Nightly Maintenance apply rollout remains a separate, unauthorized decision.
- **Intent-aware / Selective Recall deterministic authority experiment** is `REJECTED / CLOSED / NOT RUNTIME AUTHORIZED`. The first fresh C2 holdout produced V1 `23/0/24/1` and selective `22/5/19/2`; hard safety failed (`unsafe SAFE_SKIP=1`, introduced FN `1`, SAFE_SKIP precision `0.8333`) while utility passed (`5` FP reduced, rate `0.2083`). C2-C repair is `CANCELLED / DO NOT START`; taxonomy, evidence, evaluators, and corpora remain retained experimental artifacts.

### LATER

#### memory-engine 1.1 later route

- **1.1-Q4 Recall Hint v1 — `LATER / NOT STARTED`**. Treat hints only as candidate expansion and soft weighting metadata (`memory_kind`, project, entities, episode, time range/relation, query facets, expected evidence count). Original query is always retained; unknown fields remain empty; failures fall back to the original retrieval path. Hint output cannot grant recall, skip, hard-filter, or deterministic policy authority and cannot reopen the rejected full semantic classifier or Selective Recall deterministic-authority routes.
- **1.1-Q5 Statistical LTR — `LATER / NOT STARTED`**. Entry requires hundreds of real labeled queries, stable candidate channels, relevance labels, a failure taxonomy, train/validation separation, a frozen pairwise/listwise contract, and benchmark-gold leakage checks. Optimize complete, correct, current evidence within a `3–5` item budget rather than Recall@50.
- **1.1-Q6 Shadow AutoRecall — `LATER / NOT AUTHORIZED`** and **1.1-Q7 Owner-only Runtime Canary — `LATER / NOT AUTHORIZED`**. Shadow recording, telemetry creation, AutoRecall/topK changes, candidate deployment, and persistent activation each require separate runtime authorization.
- **Benchmark B6** remains an evaluation branch, not a 1.1 blocker. B6-S1 local synthetic HTTP/Docker qualification is `PASS / CLOSED / NO REAL PROVIDER`; B6-S2 official AML smoke and B6-S3 full evaluation remain `NOT AUTHORIZED`.

The following routes remain closed or outside the 1.1 main line: full semantic intent classifier, Selective Recall deterministic authority, skip-only deterministic gate, RH1, H2 bounded multi-query, unconditional semantic/vector calls, unconditional multi-query, REDACTED_CARD, INTERNAL_AGENT_CONTEXT, RAW_DISCLOSABLE/OpenSpec 4.4, multi-agent memory architecture, and external knowledge-base integration. Historical records remain preserved; this prioritization does not rewrite them.

After the canonical semantic contract is stable, advance Intelligent Recall in this order:

1. **D.3-C.16 INTERNAL_AGENT_CONTEXT Holdout First Run** — `PASS / FIRST-RUN EVIDENCE ACCEPTED`; the frozen C.14 fixture was read exactly once under the separate execution authorization, with no retry or second execution.
2. **D.3-C product interpretation** — `ACCEPTED`; the evidence supports `INTERNAL_AGENT_CONTEXT` representation only when caller-supplied ranges are already correct and does not prove automatic range selection or capability.
3. **D.3-D entry** — `APPROVED FOR DIRECT_CARD SOURCE MIGRATION ONLY` as the historical product-entry decision; the separately authorized H2-E source migration and subsequent runtime qualification are recorded below, with current status `PASS / RUNTIME QUALIFIED / CLOSED`.
4. **D.3-D.1 DIRECT_CARD Production Boundary Migration Design** — `PASS_WITH_FINDINGS`; at that historical decision point, direct-card source migration was `HOLD` with historical blocker `PRODUCTION_SAFE_TO_DISCLOSE_AUTHORITY_MISSING`. The design is docs/OpenSpec-only and does not authorize source work.
5. **D.3-D.2 DIRECT_CARD Safe-to-Disclose Authority Decision** — `PASS / DECISION CLOSED`; v1 positive authority is `OWNER_EXPLICIT_ATTESTATION` bound to surface `DISCLOSURE_CARD` and actual artifact kind `legacy_memory_card_v1`. At that historical decision point the provider was not implemented, with historical blocker `PRODUCTION_DISCLOSURE_ATTESTATION_PROVIDER_NOT_IMPLEMENTED`; D.3-D.3 subsequently implemented the repository source boundary. The source-stage `HOLD` under the D.3-D.4 audience-boundary blocker is historical; D.3-D.4 pre-implementation review is complete, and the current runtime qualification is closed.
6. **D.3-D.3 DIRECT_CARD Owner-Attested Authority Source Implementation** — `IMPLEMENTED / REPOSITORY-TESTED`; the same-stage corrective patch closes the command-object registration and meaningful-preview findings. The Engine-owned store, exact validator/provider, Canonical-source-derived Owner projection, and host-authenticated management command are implemented, but no AutoRecall wiring or runtime deployment occurred.
7. **D.3-D.4 DIRECT_CARD Production Disclosure Boundary Migration** — the historical pre-H2-E review was `AUTHORIZED / PRE-IMPLEMENTATION BLOCKED` under `PRODUCTION_OWNER_SELF_AUDIENCE_AUTHORITY_UNAVAILABLE_AT_PROMPT_INJECTION_BOUNDARY`; H2-E is the separately authorized source consumer stage and does not authorize runtime activation or host remediation.
7.1. **D.3-D.4-H1 OpenClaw Pre-Prompt Owner Audience Contract Design** — `PASS_WITH_FINDINGS / CONTRACT DESIGN FROZEN`; at the H1 design-freeze point the selected event-local contract was design-only, finding `OPENCLAW_AUTHORITATIVE_SOURCE_CHECKOUT_NOT_PRESENT_LOCALLY`, and H2 was `CANDIDATE / NOT AUTHORIZED`. Later H2, H2-E, and runtime qualification superseded that historical state; current D.3-D.4 / OpenSpec 4.3 is `PASS / RUNTIME QUALIFIED / CLOSED`.
7.2. **D.3-D.4-H2-E DIRECT_CARD Pre-Prompt Owner Audience Consumer** — `PASS / SOURCE IMPLEMENTED / REPOSITORY-TESTED`; host H2 commit `2e67ab06b6f7f1cf7655d0a8d508fc2a2ad78176` was verified read-only, and the implementation uses exact current Canonical reads, Owner projection/attestation, hard-deny capability checks, selection-only cards, no-fallback card mode, and selection-derived telemetry. At this source-stage closeout OpenSpec 4.3 and 4.4 were outside scope; 4.3 is now `PASS / RUNTIME QUALIFIED / CLOSED`, while 4.4 remains unchecked. Record: `docs/direct-card-pre-prompt-owner-audience-consumer-implementation-v1.md`.
8. **Learned/statistical recall policy** — `LATER / NOT STARTED`; requires sufficient real labeled traffic and a separate Planner decision.
9. **Recall Hint**.
10. **Statistical LTR**.

Multi-agent memory architecture may begin after the Canonical Object Contract is stable and need not wait for LTR. Its detailed ownership, visibility, attribution, and ACL semantics remain a later product-design decision.

AutoRecall broad rollout remains a separate later decision. A naturally occurring AutoRecall success/failure with independent answer-bearing evidence may still justify one bounded read-only first-loss attribution, but provenance availability alone does not reopen retrieval tuning or dedicated canary chains.


### DEFERRED

- Candidate-Builder authority publication / `npm.ci_candidate` timeout diagnosis. The committed `300000ms` inner / `330000ms` outer policy remains unchanged; additional harness micro-stages are not planned.
- Diagnostic/path-authority debt identified by the 2026-08-04 low-coverage audit. It is real but was non-causal to the H6 rejection set and does not preempt the NOW decision.

### REFERENCE ONLY

The historical B8-A7 sustained-production-evidence and strict platform-profile rollout work remains available as design and operational reference. It is not the current roadmap and does not authorize sustained runtime, removal-gate work, or a rollout continuation.

## Workstreams

- Retrieval correctness: preserve candidate provenance, filtering, ordering, and fallback contracts.
- Storage ownership: keep Core reads separate from Engine-owned writes and enforce database boundaries.
- Confidence lifecycle: maintain explicit managed-confidence, external-candidate, decay, archive, conflict, and reinforcement semantics.
- Safety gates: keep opt-in behavior, fail-closed paths, bounded operations, and rollback conditions explicit.
- Observability: expose bounded, privacy-safe diagnostics and stable debug metadata for verification.
- Benchmark and quality evidence: preserve internal fixtures as regression evidence, freeze external baseline profiles, record exact dataset/version provenance, compare retrieval quality and latency across versions, and require cross-dataset evidence before benchmark-driven heuristics become product policy.
- Executable engineering gates: keep one repository-owned acceptance command that composes syntax, behavior, contract, test-integrity, diff, and applicable OpenSpec checks; prevent empty-shell tests and prose-only PASS records from satisfying acceptance; independently spot-check the resulting evidence with DevSpace.
- Release verification: compare source, artifacts, configuration semantics, and test evidence before deployment.
- Deployment rollback discipline: use explicit authorities, reversible steps, and verified rollback targets for any authorized runtime transaction.
- Runtime authority drift checks: scope worktree cleanliness to files that can affect the authorized execution path; do not fail a runtime qualification solely because unrelated documentation, test, report, or roadmap files are modified. Keep exact Stage Card SHA and frozen runtime-source hashes as separate authority checks. For the current session-checkpoint/persistent-runtime path, the drift surface is `bin/`, `lib/`, repository-root `*.js`/`*.cjs`, `package.json`, `package-lock.json`, and `openclaw.plugin.json`.
