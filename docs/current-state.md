# memory-engine Public Current State

> Status: `Current public source state`
>
> This public document records current product/source decisions and bounded
> qualification facts explicitly accepted by the project; it does not
> generalize them to unrelated private deployments.
>
> It describes current product/source decisions only and does not pin its own containing Git commit. Use `git rev-parse HEAD` for exact repository authority.

## Public source contracts

- OpenClaw Core storage is read-only from memory-engine; writes to Core-owned data are prohibited.
- The Engine database owns confidence and event lifecycle state.
- External candidates are distinct from managed candidates and do not inherit managed-confidence semantics.
- AutoRecall is disabled by default.
- Retrieval and runtime changes require explicit testing and deployment authorization.
- `accepted_design`: the memory-engine `1.0.0` product boundary is the L2 stable production baseline: Core/Engine storage ownership, the Canonical Memory semantic boundary and canonical writer path, bounded `memory_engine_search`, Owner-only explicit full-content `memory_engine_get`, and the qualified DIRECT_CARD disclosure boundary. Learned/statistical recall, Recall Hint, Statistical LTR, Multi-agent memory, `RAW_DISCLOSABLE`, REDACTED_CARD production integration, and broad AutoRecall rollout remain separate 1.x product decisions rather than 1.0 completion requirements. AutoRecall and card-first runtime remain disabled by default. Official release identity is still determined by the nearest reachable release tag rather than by this document.

## Current product-source baseline

- `current_fact`: the latest product-source change in the Candidate-Builder line is `97454ee70f47f8fd4421806f4a10100b78e27186` (`fix(runtime): bound npm ci sandbox timeout`). Later commits through the 2026-08-08 governance closeout are documentation/runtime-gate records, not additional Candidate-Builder product-source changes.
- `current_fact`: `npm.ci_candidate` uses the closed inner `300000ms` / outer `330000ms` timeout policy. Ordinary registered sandbox operations remain `120000ms` / `120000ms`; the capability probe keeps its separate `120000ms` inner / `30000ms` outer bounds.
- `current_fact`: post-timeout-fix verification recorded focused timeout/prepare tests `20/20`, production E2E `17/17`, runtime-authority tests `79/79`, static check over `614` files, and the Node 24 / Asia-Shanghai full suite `1832` passed / `0` failed / `8` skipped.
- `current_fact`: the session-flush reconciliation product-source line is implemented at `ac0e5f054551847e724be504bae80947abd7d675` (`feat(recall): reconcile session-flush managed memory`), including the dedicated `lib/checkpoint/session-flush-reconciliation.js` path and its focused regression coverage.

## Current product decisions

- `historical_record`: H6 and its retry both closed `INSUFFICIENT_EVIDENCE`; they are separate samples and must not be combined into a PASS claim.
- `current_fact`: the completed low-coverage/path-authority audit at `reports/low-coverage-path-authority-audit-20260804/final-report.md` concluded `AUDIT_OUTCOME=NATURAL_CONTENT_LOW_OVERLAP` with high confidence, reproduced the ten historical gate decisions, rejected a reproducible gate defect, and found diagnostic/path-authority debt to be non-causal to those H6 rejections.
- `current_fact`: the authorized H6 answerability/memory-coverage attribution audit closed `PASS_WITH_FINDINGS` on 2026-08-08. Seven of nine rejected turns were directly attributable to `RETRIEVAL_SELECTION_GAP`; none was proven `CORPUS_COVERAGE_GAP` or `PROJECTION_BOUNDARY_GAP`. First H6 T1 exposed an unmodeled gate-semantic boundary because the selected first-240-character payload already contained the correct `AutoRecall=false` fact, while retry T3 remained incident-causality `unproven`.
- `current_fact`: the separately authorized H6 retrieval first-loss-boundary audit closed `PASS_WITH_FINDINGS` on 2026-08-08 with aggregate `MIXED_OR_INSUFFICIENT_EVIDENCE`. Historical debug proves FTS did not preserve the established answer-bearing chunks and does not support a common query-formation defect, but vector/recent telemetry retained only candidate counts rather than per-channel candidate IDs, so channel/index loss cannot be distinguished from fusion/preselection loss for the seven turns.
- `current_fact`: bounded AutoRecall candidate-provenance observability was implemented and committed at `1cd183ff12d055ba5c5ecd4bd0d9d1b98cdff23b`; focused/privacy tests passed `19/19`, and the Node 24 full suite passed `1835`, failed `0`, skipped `8`.
- `historical_record`: the first provenance runtime-install transaction closed `STOPPED` during candidate construction before Gateway stop or plugin install. Its candidate `provenance-overlay-1cd183ff-20260808` remains an immutable byte-identical sealed clone of rollback.
- `historical_record`: the separately authorized R2 runtime retry closed `PASS_WITH_FINDINGS` under private execution evidence: the provenance overlay installation/non-live contract passed, bounded/privacy projector checks passed, config semantics were unchanged, and AutoRecall was restored to disabled. This public document intentionally does not publish private runtime paths or identities.
- `current_fact`: OpenClaw's EDi identity currently resolves to actual `agentId=main`; the legacy/default AutoRecall allowlist value `edi` would therefore deny the intended EDi runtime traffic unless an execution explicitly binds `main`.
- `historical_record`: the first separately authorized natural-canary execution closed `STOPPED` before AutoRecall enablement. Its bootstrap turn successfully created the exact dedicated `main` session while AutoRecall remained disabled; execution then stopped because the verifier read the session-list field as `id` instead of `sessionId`. Closeout also exposed invalid multiline shell-test syntax and a false scope-leak count caused by an uncaptured event baseline defaulting to zero. Config/runtime remained on the pre-canary baseline and no natural canary retrieval ran.
- `historical_record`: natural-canary retry R2 closed `STOPPED`. It successfully enabled the exact scoped AutoRecall config and produced technically valid bounded provenance, scope isolation, and exact config restoration, but no genuine Sol question was submitted: the harness ran through `bash -s <<'EOF'` and plain `read` consumed remaining heredoc script text, sending `STAGE_RESULT="INSUFFICIENT_EVIDENCE"` as the first prompt. That accidental prompt is not natural-use evidence.
- `current_fact`: final natural-canary retry R3 closed `PASS` under separately verified execution evidence. One genuine `/dev/tty` user turn reached the exact dedicated `main` session; post-baseline AutoRecall persisted bounded per-channel and fusion candidate provenance, no foreign session entered `recall_started`, and the exact pre-canary config was restored with AutoRecall disabled. The stage result is recorded in `reports/auto-recall-provenance-natural-canary-retry-r3/final-report.md`.
- `accepted_design`: bounded candidate-provenance observability is closed as demonstrated on genuine natural-use runtime traffic. Retrieval thresholds, query shaping, FTS/vector/recent policy, fusion/ranking, Card/gate policy, capture behavior, and broad AutoRecall rollout remain frozen. No R4 or additional dedicated canary is planned. The next bounded decision is only a later read-only first-loss attribution when a naturally occurring success/failure has independent answer-bearing evidence sufficient to locate the loss boundary; provenance existence alone does not authorize retrieval tuning.
- `accepted_design`: Candidate-Builder publication/timeout diagnosis is deferred. Existing harness evidence remains historical; no additional harness micro-stage, timeout change, real plan/dry-run/prepare/verify, runtime mutation, or rollout is authorized without a new product-level decision.
- `historical_record`: the separately authorized Session-Flush Reconciliation Persistent Rollout Successor closed `PASS` on 2026-08-17. Its retained final report records successful candidate activation, one bound natural scheduled `session-checkpoint` lifecycle, persistence after that lifecycle, healthy Gateway state, and AutoRecall remaining disabled. The persistent-activation qualification line is closed; no R5 or additional persistent-activation qualification is required.
- `current_fact`: L2 Database Boundary Closure is **deployed runtime qualification PASS / L2 COMPLETE** at active runtime source parity `057f43e`. Gateway `READY`; `AutoRecall=false`; Hybrid runtime `KG_ACCESS_MODE=isolated`, `RECENT_ACCESS_MODE=isolated`, and `production legacy fallback=0`. The current Nightly dry-run isolated topology is `PASS`, with no unexpected memory/confidence mutation. The production DB runtime no longer exposes the combined Engine+attached-Core `withDb` / `openDb` capability; plugin, canonical CLI service, Console, current Nightly Maintenance command, and session-checkpoint paths use explicit isolated Core and Engine handles. Production Hybrid scope does not expose `withLegacyDb`; KG/Recent guard failure fails closed instead of executing combined fallback. Normal action/get/status, startup legacy-event migration, AutoRecall reinforcement, Console unified-event views, and conflict detection split Core-readonly and Engine-only operations.
- `historical_record`: a historical/pre-existing tool-policy observation is independent of L2: `tools.catalog` registration was complete for `memory_engine`, `memory_engine_search`, and `memory_engine_get`, while `tools.profile=coding` caused `tools.effective` to filter those tools from the `main` model. At that historical point, no persistent `alsoAllow` expansion was present. This had no causal relationship to L2 DB boundary qualification.
- `current_fact`: `TOOL_POLICY_AVAILABILITY=PASS / CLOSED`. A main-only scoped `alsoAllow` is deployed and runtime-verified: `memory_engine_search` and `memory_engine_get` are visible to the `main` model, while the broad `memory_engine` management router remains registered but hidden from the main model by default. The new Gateway/main-agent tool-definition set includes search/get, and the main agent directly called `memory_engine_search` successfully rather than using operator `tools.invoke`. Researcher, coding, dispatcher, task-planner, and codex agents did not receive search/get; unexpected plugin tools were `none`. `tools.profile` remains `coding` rather than `full`; `AutoRecall=false`; no memory content or confidence-lifecycle mutation occurred; the only expected write was Hybrid search telemetry. Other agents remain unaffected.
- `current_fact`: Phase 2.5-A is `PASS / CLOSED`; Phase 2.5-B is `PASS / CLOSED`; Phase 2.5-C is `PASS_WITH_FINDINGS / CLOSED`; Phase 2.5-D source, deployment, and runtime qualification are `PASS / QUALIFIED`; Phase 2.5-D.1 source and deployment are `PASS / CLOSED`, with live writer qualification `PASS / QUALIFIED`; and overall Phase 2.5 Canonical Memory Architecture is `PASS / CLOSED` under `canonical-memory-architecture`.
- `current_fact`: Conflict Ownership Closure is `SOURCE CLOSED / PASS`: Session Checkpoint retains exclusive ownership of `category='preference'` conflict flags through `resolvePreferenceConflicts()` / `preference_latest_wins`; generic lifecycle detectors exclude preference while preserving non-preference related-conflict behavior. No schema change was made. Nightly Maintenance apply rollout remains unauthorized and is a separate decision from this source closure.
- `current_fact`: Intent-aware Recall v2-A is `SOURCE IMPLEMENTED / VERIFIED`: deterministic shared `task_intent` / `recall_intent` classification is runtime-observational, frozen 12-row gold-set replay enforces both labels, and decision trace/AutoRecall debug metadata expose only bounded enum values. Existing recall decisions, focused queries, Hybrid policy, ranking, Card/Get behavior, and schema version 1 remain unchanged. AutoRecall remains disabled; no runtime deployment is implied. V2-B policy coupling remains a separate product decision.
- `historical_record`: Intent-aware Recall v2-B1 policy evaluation initially scored the independent 36-row design fixture at V1 `18 TP / 5 TN / 13 FP / 0 FN`, oracle `18 / 18 / 0 / 0`, and runtime candidate `8 / 18 / 0 / 10`; it found 4 task-intent and 12 recall-intent mismatches. The fixture and labels were not changed during B2.
- `current_fact`: Intent-aware Recall v2-B2 is `SOURCE IMPLEMENTED / KNOWN-GAP REGRESSION CLOSED`: generalized deterministic continuation, implicit project-state, prior-decision, and historical-entity lookup signals now produce zero task/recall mismatches on the frozen B1 fixture. V1 remains `18 TP / 5 TN / 13 FP / 0 FN`; oracle and runtime candidate each score `18 TP / 18 TN / 0 FP / 0 FN` with accuracy, precision, and recall `1`. The candidate mapping remains not runtime authorized; no production `should_recall`, `intent_reason`, focused-query, retrieval/presentation policy, config, DB, or runtime behavior changed.
- `historical_record`: Intent-aware Recall v2-B3's first independent run used the immutable `ceded7d7a629cb35817fe9a1b95d6849e55c8256` 48-row holdout freeze and closed `PASS_WITH_FINDINGS / HOLDOUT NOT READY`: V1 `24 TP / 0 TN / 24 FP / 0 FN`, oracle `24 / 24 / 0 / 0`, and runtime `7 / 19 / 5 / 17` with precision `0.5833` and recall `0.2917`; task mismatches were `14` and recall mismatches `25`. This historical result is not rewritten by B4.
- `current_fact`: Intent-aware Recall v2-B4 is `SOURCE IMPLEMENTED / STRUCTURED EVIDENCE REGRESSION CLOSED`. A pure bounded request-scope/evidence layer masks quoted or supplied content, gives explicit history suppression/current-input-only evidence precedence, requires structured lookup relations beyond project/entity anchors, and lets task classification use the request surface. The frozen 12-row seed remains `12/12`; B1 remains V1 `18/5/13/0` with zero task/recall mismatch and runtime candidate `18/18/0/0`; B3 regression is V1 `24/0/24/0` with zero task/recall mismatch and runtime candidate `24/24/0/0`. These are known-regression results, not generalization qualification.
- `current_fact`: After B4 tuning, the B3 corpus has evidence role `known_regression_after_v2b4` and current status `REGRESSION ONLY / NOT INDEPENDENT READINESS EVIDENCE`; its historical status remains `PASS_WITH_FINDINGS / HOLDOUT NOT READY`. The candidate mapping remains **NOT RUNTIME AUTHORIZED**, AutoRecall remains disabled, and no production policy, runtime, config, data, DB, or deployment change is implied.
- `historical_record`: the 2026-08-19 Phase 2.5-D.1 runtime baseline used memory-engine source `3ce61697ef2ef77b91b60e229b0924b074038d86`; Gateway was `READY` on OpenClaw `2026.6.9`, `AutoRecall=false`, and the then-deployed `lib/tools/memory-engine-actions.js` SHA-256 was `468a03f7c78e0a219e1899a54fc8e318f4a711e44828357869e13b81f9bb9585`. Later DIRECT_CARD and explicit-tool deployments supersede that runtime-file state; the historical Phase 2.5-D.1 qualification itself remains valid.
- `historical_record` (2026-08-19, corrected/withdrawn): the earlier Core observation-gap conclusion used the legacy Core database instead of the authoritative main-agent Core database; it is withdrawn as `EVIDENCE ERROR`. Runtime path resolution has preferred the agent database when present since `eaf6993`, but the earlier verifier queried the legacy location. The first controlled marker `PHASE25D_CANONICAL_WRITER_SMOKE_20260819T1254_057f43e` had exact Core ID `1a47d2e6a183a2c884ff85637a0bc66251288e60b57cc5d11df40a73d73bdd72`, and the actual product failure was Engine-row absence being used as Core identity discovery after sync backfill had already created the Engine row. D.1 fixed that coupling.
- `historical_record` (2026-08-19 final live qualification): marker `PHASE25D1_FINAL_WRITER_SMOKE_20260819T1521_3ce6169` produced exact Core ID `0c99335a14d6bb77d81e716dde49495b8d6552788b2a922976e2942738ad81eb`. Source, Core, Engine, `memory_created`, and Lance each had one exact-ID row; Lance text matched the 526-character Core canonical projection; event metadata was `category=temporary`, `confidence=0.4`, `tau=2`, `lance_written=1`, `derived_state=complete`, `needs_reconcile=false`, `vector_error=null`. A later collision-gate stop was a correct duplicate guard, not a failed qualification; no reconciliation was required.
- `finding`: `ADD_SYNC_BACKFILL_SCOPE_FINDING` remains a non-blocking operational/lifecycle finding: the first live add triggered `backfill.inserted=111`, while D.1 intentionally changed none of sync, confidence backfill, reconciliation, or cleanup. Final preflight converged at eligible Core `815`, Engine confidence `815`, missing confidence `0`.
- `finding`: final Core rechunking left one stale Engine confidence row, exact ID `1a47d2e6a183a2c884ff85637a0bc66251288e60b57cc5d11df40a73d73bdd72`; eligible Core `815`, Engine confidence `816`, missing confidence `0`, orphan confidence `1`. This is non-blocking lifecycle maintenance, not data corruption or a Phase 2.5 blocker; no cleanup was run.
- `historical_record`: v2-B5 fixture `test/fixtures/auto-recall-policy-holdout.v2b5.jsonl` was frozen before evaluation at commit `926f034`. It contains 48 Planner-specified rows across 12 families, balanced 24/24, with schema version 1, high-confidence labels, the `v2b5_planner_holdout` annotator marker, and no mixed `none` arrays; the fixture is immutable after freeze.
- `current_fact`: Intent-aware Recall v2-B5 fresh independent holdout evaluation is `PASS_WITH_FINDINGS / HOLDOUT NOT READY`. The contract and oracle gates passed; V1 scored `24 TP / 1 TN / 23 FP / 0 FN`; oracle scored `24 / 24 / 0 / 0`; runtime candidate scored `3 / 17 / 7 / 21` with precision `0.3` and recall `0.125`. Task mismatches were 16, recall mismatches were 29, semantic-only mismatches were 7, and 8 families exceeded the one-error concentration bound. This is independent evaluation evidence and a Planner finding, not classifier-tuning authorization or runtime policy approval.
- `current_fact`: B5 evaluator status is `PASS / OFFLINE ONLY`. The candidate mapping remains **NOT RUNTIME AUTHORIZED**; no B5 classifier/evidence/fixture tuning, production policy change, deployment, AutoRecall enablement, config/data mutation, or runtime mutation occurred.
- `current_fact`: Planner final adjudication closes the v2-B full deterministic semantic production-authority route as **REJECTED / CLOSED / NOT RUNTIME AUTHORIZED** after independent B3 and B5 generalization failures. v2-B6 production policy-authority review is **CANCELLED / DO NOT START**. The intent taxonomy and oracle mapping remain useful for annotation, diagnostics, telemetry, gold-set work, and future learned/LTR supervision.
- `historical_record`: v2-C1 `selective-recall-gate` was source/offline only. Its skip-only candidate used `SAFE_SKIP` / `ABSTAIN`, preserved V1 on abstention, and reduced known-corpus aggregate FP `60→52` with `0` introduced FN (`B1 13→13`, `B3 24→17`, `B5 23→22`). These were design/regression results only.
- `historical_record`: v2-C2-A prepared the behavioral holdout contract, and v2-C2-B froze `test/fixtures/selective-recall-gate-holdout.v2c2.jsonl` with 48 rows, 24/24 balance, and 12 families at 2 YES/2 NO each. The fixture and source remained immutable through evaluation.
- `historical_record`: the first fresh independent C2 evaluation was `PASS_WITH_FINDINGS / HOLDOUT NOT READY`: V1 `23 TP / 0 TN / 24 FP / 1 FN`; selective `22 TP / 5 TN / 19 FP / 2 FN`; `SAFE_SKIP=6`, safe correct `5`, unsafe `SAFE_SKIP=1`, precision `0.8333`, introduced FN `1`, FP reduced `5`, reduction rate `0.2083`. Hard safety failed while utility passed; this was `FAIL FOR PRODUCTION AUTHORITY`.
- `current_fact`: Planner final adjudication closes the deterministic Intent-aware Recall / Selective Recall authority experiment as **`REJECTED / CLOSED / NOT RUNTIME AUTHORIZED`**. The fresh failure is not treated as a missing-token defect: absence of detected history-reference evidence is not sufficient proof that a mixed natural-language request has no history requirement. C2-C repair is **`CANCELLED / DO NOT START`**; no classifier or regex tuning is authorized.
- `current_fact`: the intent taxonomy, structured evidence, and C1/C2 evaluators remain observational/experimental artifacts, and B1/B3/B5/C2 remain retained corpora. The deterministic Intent-aware Recall / Selective Recall production-authority route remains closed; learned/statistical recall policy remains later work and is not opened by the disclosure work.
- `historical_record`: Retrieval-first Selective Disclosure Phase D.2-B evaluated the frozen v2 candidate-level holdout and closed `FAILED / PRODUCT ARCHITECTURE GAP`: `48` rows, `24` answer-bearing candidates, `41` selected cards, `7` withheld, `23` unsafe disclosures, `0` unauthorized full-content surfaces, and answer-bearing disclosure recall `0.75`. The fixture, selector, admissibility policy, and evaluator were unchanged; no runtime authority was granted.
- `historical_record`: Phase D.2-C.7 offline shadow evaluation compared the current and first capability-constrained paths without changing source or fixture: both selected `41` cards, withheld `7`, disclosed `23` unsafe candidates, and had answer-bearing recall `0.75`; unsafe-disclosure reduction was `0`. This preserved the product architecture finding and was not runtime authorization.
- `accepted_design`: Disclosure Capability Contract v1.1 is `PASS / CONTRACT CLOSED`. It separates `RETRIEVAL_ONLY`, `INTERNAL_CONTEXT`, `CARD_DISCLOSABLE`, and reserved `RAW_DISCLOSABLE`; `CARD_DISCLOSABLE` requires active lifecycle, valid projection, allowed scope, acceptable risk, and `safe_to_disclose=true`. `safe_to_disclose` belongs to capability calculation, while `safe_to_disclose=false` maps to `INTERNAL_CONTEXT` when internal use remains allowed and to `RETRIEVAL_ONLY` when blocked or unusable. The contract does not itself authorize production integration or runtime use.
- `current_fact`: Phase D.2-C.10 implemented the pure offline Disclosure Capability Shadow Evaluator v1.1 at commit `8db96038cb44818df2674b5f3b87a99adab93728`. The evaluator feeds the bounded `safe_to_disclose` label into shadow capability calculation, preserves the existing selector as the presentation mechanism, never emits `RAW_DISCLOSABLE`, and performs no retrieval, DB write, network call, LLM call, production selector change, or runtime mutation.
- `historical_record`: Phase D.2-C.11 re-evaluated the unchanged frozen 48-row v2 fixture with SHA-256 `d372ebac9bf4d80aaac85dfb0d43eacec792291871b744b7812009ac06d071bd`. Current-path unsafe card disclosure remained `23`; shadow v1.1 unsafe card disclosure was `0`. Answer-bearing disclosure recall remained `0.75`; selected cards changed `41 -> 18`, withheld cards `7 -> 30`, and irrelevant disclosures `23 -> 0`. This supports the capability-boundary hypothesis offline but is not production readiness or runtime authority.
- `accepted_design`: Phase D.3 Memory Projection Architecture separates projection representation from permission authority. Canonical Memory remains semantic authority; ProjectionArtifact binds exact canonical identity/content hash for an explicit surface; projection validation checks structure/bounds/leakage; Disclosure Capability remains the permission boundary; and selector remains selection-only. `SANITIZED_CARD` / generic `PROJECTABLE` are not capability states.
- `current_fact`: Phase D.3-B is `SOURCE IMPLEMENTED / VERIFIED`. `lib/canonical/projection-artifact.js` defines the pure v1 artifact envelope and adapters for the existing `VECTOR_INDEX` and canonical-aware `DISCLOSURE_CARD` projectors. Card artifacts omit legacy `disclosure_level`, `get_token`, `can_inject_card`, `can_get_full_content`, reinforcement, capability, and `safe_to_disclose` authority; validation rejects permission/capability fields, identity/hash drift, cross-surface reuse, and forbidden full-body fields. `INTERNAL_AGENT_CONTEXT` and `RAW_REFERENCE` are explicit but fail closed as not implemented. Focused canonical projection tests passed `20/20`, D.2 disclosure regressions passed `44/44`, static check passed over `671` files, and strict OpenSpec validation passed. No production source imports the new artifact module; no runtime/config/DB/data mutation occurred.
- `current_fact`: Phase D.3-C.1 is **`HOLDOUT CONTRACT FROZEN / NOT YET EVALUATED`**. The synthetic `memory-projection-holdout-v1` fixture has 24 rows across 6 families, 4 rows per family, and 2 answer-bearing / 2 non-answer-bearing rows per family. It freezes projection inputs plus independent `surface_safety.forbidden_literals` and `semantic_preservation.required_literals` constraints; actual projected payload/bounded projection features and `actual_surface_safe` / `actual_semantic_preserved` remain evaluator results.
- `accepted_design`: Projection remains representation, capability remains permission, and selector remains selection. The fixture permits `projection_feasible_but_capability_blocked`: a valid/useful `DISCLOSURE_CARD` representation does not upgrade current `RETRIEVAL_ONLY` or `INTERNAL_CONTEXT` authority to `CARD_DISCLOSABLE`. The D.2 v2 holdout is byte-identical and remains invalid evidence for projection safety.
- `current_fact`: Phase D.3-C.2 is **`SOURCE IMPLEMENTED`**. `lib/recall/disclosure/projection-aware-evaluator.js` provides pure case/cases/fixture APIs, validates actual `DISCLOSURE_CARD` artifacts, measures payload-only surface safety and semantic preservation, compares actual projection validity with the frozen expectation as consistency evidence, and adapts bounded policy context to the existing v1.1 capability explanation without importing the selector. Tests use handcrafted synthetic rows only; production source remains unchanged.
- `current_fact`: Phase D.3-C.3 first-run evidence is **`ACCEPTED BY PLANNER FOR ARCHITECTURE INTERPRETATION`**. Exactly one offline evaluation execution at HEAD `b62121773966dea56102661c1dfc45105a50bf2e` evaluated the frozen 24-row fixture: projection valid `24/24`, projection-validity expectation match `24/24`, surface safe `20/24`, answer-bearing semantic preserved `8/12` (`0.6667`), useful projection `6/12` (`0.5`), CARD-authorized useful projection `2`, capability-blocked useful projection `4`, capability expectation match `24/24`, and disclosure-authority expectation match `24/24`. The supported interpretation is that the current ProjectionArtifact contract had no structural failure on this frozen synthetic holdout, the current v1.1 capability matched its frozen expected contract, and the current card representation showed family-specific surface-safety and semantic-preservation gaps. Full bounded evidence is recorded in `reports/memory-projection-holdout-v1-first-run-20260822.md`.
- `accepted_design`: Phase D.3-C.4 is **`PROJECTION STRATEGY TAXONOMY DEFINED`**. `docs/memory-projection-strategy-taxonomy-v1.md` freezes representation strategies separately from capability authorization and selection. This is an accepted design direction, not current runtime or implementation status; no production PASS/FAIL/READY threshold or mutation is implied.
- `current_fact`: Phase D.3-C.5 is **`REDACTED_CARD OFFLINE PROTOTYPE IMPLEMENTED / NOT INDEPENDENTLY EVALUATED`**. The pure prototype accepts an explicit bounded field-specific redaction plan, uses fixed `[REDACTED]` replacement, preserves canonical/protected presentation authority fields, validates structural compatibility with the existing `DISCLOSURE_CARD` machinery, and returns no capability or authorization. Content-detection authority remains out of scope; the C.1 frozen holdout and C.3 first-run report were not reused for tuning or evaluation; the existing production card projector is unchanged.
- `current_fact`: Phase D.3-C.6 is **`INDEPENDENT REDACTED_CARD HOLDOUT FROZEN / NOT YET EVALUATED`**. The fresh synthetic fixture `redacted-card-holdout-v1` has 12 rows, 6 families, 2 rows per family, and one answer-bearing/non-answer-bearing row per family; SHA-256 is `55d1d876b111bc7e3dcc7dcadb6761721cb6b05568edc27cc0ff1d81de5a5d48`. The pure validator freezes intended-valid redaction plans plus independent forbidden-literal, semantic-anchor, and structural-compatibility constraints, with no expected output payload or capability labels. The prototype was not called and the fixture was not evaluated.
- `current_fact`: Phase D.3-C.7 is **`PURE INDEPENDENT REDACTED_CARD EVALUATOR IMPLEMENTED`**. `redacted-card-evaluator.js` accepts caller-supplied in-memory C.6-contract rows, measures transform success, structural compatibility, target-surface safety, answer-bearing semantic preservation, protected-field preservation, and unplanned presentation-field drift, and returns no candidate/baseline payload or authorization. Its tests use only fresh handcrafted C7 material, including an independent generated 12-row/six-family contract-valid set; the C.6 fixture was not used by C.7 tests.
- `current_fact`: Phase D.3-C.8 is **`PASS / FIRST-RUN EVIDENCE ACCEPTED`** within the frozen 12-row synthetic C.6 contract. The immutable fixture was evaluated exactly once at HEAD `b4ce5980f2e2b26962ac1bc9daa5fbc9c056edc6`: 12 cases, transform success `12/12`, structural match `12/12`, surface safe `12/12`, answer-bearing semantic preserved `6/6` (`1`), protected fields preserved `12/12`, no unplanned drift `12/12`, and useful redacted projections `6/6` answer-bearing cases (`1`). This is independent offline representation evidence only; it does not establish production readiness, runtime readiness, detector correctness, arbitrary real-world sensitive-content safety, capability relaxation, or production integration. Full bounded case/family evidence is in `reports/redacted-card-holdout-v1-first-run-20260822.md`.
- `accepted_design`: Phase D.3-C.9 is **`REDACTION PLAN AUTHORITY BOUNDARY DEFINED`** in `docs/redaction-plan-authority-boundary-v1.md`. No literal-level production redaction authority source is implemented; risk flags cannot synthesize directives; successful redaction does not upgrade capability; and no production integration is authorized.
- `current_fact`: Phase D.3-C.10 is **`STRUCTURED REDACTION EVIDENCE CONTRACT IMPLEMENTED / OFFLINE ONLY / NOT PRODUCTION AUTHORITY`**. `redaction-evidence-contract.js` provides a closed schema v1, exact Canonical Memory identity binding, `DISCLOSURE_CARD`-only baseline validation, deterministic baseline projection hashing, bounded stale/target/ref/directive diagnostics, and validation/assert APIs. Structural validity is not authorization; no resolver, detector, production plan source, capability, selector, or runtime flow exists.
- `accepted_design`: Phase D.3-C.11 is **`REDACTION EVIDENCE RESOLUTION SEMANTICS DEFINED`** in `docs/redaction-evidence-resolution-semantics-v1.md`. Future resolution requires every claim to pass C.10 against one identity/baseline, fails the entire set on invalid/stale evidence or conflict, defines no implicit authority precedence, isolates detector evidence as research-only, and treats consistent eligible claims as non-authorizing semantics. No resolver, authenticated evidence-origin authority, production redaction-plan source, capability, selector, or runtime authority exists.
- `accepted_design`: Phase D.3-C.12 is **`INTERNAL_AGENT_CONTEXT REPRESENTATION BOUNDARY DEFINED`** in `docs/internal-agent-context-representation-design-v1.md`. It records the C.3 raw_log/tool_output evidence gap, keeps strategy/surface/capability separate, chooses bounded deterministic extractive evidence as the first research direction, and freezes untrusted-evidence consumer semantics. No INTERNAL_AGENT_CONTEXT projector, ProjectionArtifact source change, capability/selector change, runtime wrapper, or production integration exists.
- `current_fact`: Phase D.3-C.13 is **`INTERNAL_AGENT_CONTEXT PROJECTION CONTRACT PROTOTYPE IMPLEMENTED / OFFLINE ONLY / NOT CAPABILITY AUTHORIZED`** in `docs/internal-agent-context-projection-contract-v1.md`. The existing ProjectionArtifact module now provides a bounded source-derived character-range adapter and validator for the `INTERNAL_AGENT_CONTEXT` surface, with `content_role=untrusted_evidence`, inherited canonical identity, explicit segment/count bounds, and caller-supplied risk metadata pass-through. No production caller, segment selector/detector, summarizer, capability/selector change, runtime consumer, or holdout evaluation exists.
- `current_fact`: Phase D.3-C.14 is **`INDEPENDENT INTERNAL_AGENT_CONTEXT HOLDOUT FROZEN`** in `docs/internal-agent-context-holdout-v1-freeze.md`. The fresh `internal-agent-context-holdout-v1` contract has 12 synthetic rows across six balanced families, with independent IACH1 namespace, caller-supplied ranges, source-slice semantic constraints, instruction-data labels, and risk-flag preservation labels. The freeze contract remains immutable; its separately authorized C.16 first-run evidence is recorded below.
- `current_fact`: Phase D.3-C.15 is **`PURE INDEPENDENT INTERNAL_AGENT_CONTEXT EVALUATOR IMPLEMENTED`** in `docs/internal-agent-context-evaluator-v1.md`. The evaluator accepts only caller-supplied in-memory rows, measures bounded source-faithful representation axes, and reports instruction/data behavior as representation-level evidence. Its handcrafted tests generate fresh unit rows and a fresh in-memory 12-row contract-valid set; no production caller, capability, selector, runtime, or AutoRecall integration exists.
- `current_fact`: Phase D.3-C.16 is **`PASS / FIRST-RUN EVIDENCE ACCEPTED`**. At execution HEAD `f5cae8c321e588e8ccd9a2844a2623aac0599255`, the frozen C.14 fixture was passed through `evaluateInternalAgentContextFixture(rows)` exactly once with no warmup, sample, partial, family-by-family run, or retry. The accepted evidence is bounded extractive `INTERNAL_AGENT_CONTEXT` representation independently generalized across the frozen C.14 synthetic families when caller-supplied source ranges are already correct: projection success `12/12`, projection validity `12/12`, boundedness `12/12`, source faithfulness `12/12`, answer-bearing semantic preservation `6/6`, instruction-like representation validity `2/2`, risk metadata preservation `12/12`, provenance preservation `12/12`, no capability authority `12/12`, and useful internal projections `6/6`. This does not prove automatic range selection. Full evidence is in `reports/internal-agent-context-holdout-v1-first-run-20260823.md`; no C.17 was created.
- `accepted_design`: D.3-C product interpretation is **`ACCEPTED`** and D.3-D entry is **`APPROVED FOR DIRECT_CARD SOURCE MIGRATION ONLY`** as of 2026-08-23. `DIRECT_CARD` is the only current production integration candidate; `REDACTED_CARD` and `INTERNAL_AGENT_CONTEXT` remain `HOLD / RESEARCH_ONLY`, `REFERENCE_ONLY` and `SUMMARIZED_CARD` remain deferred research strategies, and `RAW_REFERENCE` / `RAW_DISCLOSABLE` remain reserved/disabled. This is not source implementation, deployment, runtime activation, or runtime/data authorization. Decision record: `docs/memory-projection-product-interpretation-v1.md`.
- `current_fact`: The production AutoRecall path now branches on `cardFirstRuntimeEnabled`. The enabled card branch is `before_prompt_build -> hybridSearch() -> shouldInjectCandidate() -> evaluateDirectCardProductionBoundary() -> selectDirectCardCandidates() -> formatDirectCardContext()`, while the disabled branch retains `formatAutoRecallContext()` unchanged. The direct branch consumes only `event.senderIsOwner === true`, reads exact `candidate.memory_id` Canonical objects in the isolated read-only Hybrid scope, derives the Owner ProjectionArtifact, checks exact active attestation plus lifecycle/scope/risk authorities, and has no raw or legacy-card fallback.
- `historical_record`: D.3-D.1 design was **`PASS_WITH_FINDINGS`**. At that historical decision point, `DIRECT_CARD` production source migration was **`HOLD`** with blocker **`PRODUCTION_SAFE_TO_DISCLOSE_AUTHORITY_MISSING`**. Existing isolated Hybrid search already performed bounded Core/Engine canonical reads and returned reduced results without `canonicalMemory.source.text`, but no production target-surface-specific `safe_to_disclose` authority provider existed. Decision record: `docs/direct-card-production-boundary-migration-design-v1.md`.
- `historical_record`: D.3-D.2 closed as **`PASS / DECISION CLOSED`** when no production disclosure attestation provider existed. At that historical decision point, the production migration blocker was **`PRODUCTION_DISCLOSURE_ATTESTATION_PROVIDER_NOT_IMPLEMENTED`**. D.3-D.3 subsequently supplied an Engine-owned source provider and Owner management boundary in repository code; before H2-E, it was not wired into the production disclosure path and the source migration was **`HOLD`** under the audience-boundary blocker. H2-E then supplied the repository consumer source path; the later 2026-08-24 qualification record below supersedes that pre-runtime status.
- `accepted_design`: The sole v1 positive safe-to-disclose authority is **`OWNER_EXPLICIT_ATTESTATION`**, created only by a host-authenticated, non-agent-callable Owner management boundary. It must bind exact `memory_id`, `canonical_id`, `source_content_hash`, surface `DISCLOSURE_CARD`, the actual artifact kind `legacy_memory_card_v1`, projection schema and exact projection/baseline hash, adapter/provenance version, authority kind, `OWNER_SELF` audience, policy/attestation versions, and active/revoked state. Missing, stale, revoked, invalid, or mismatched attestation means `safe_to_disclose=false/absent`, `RETRIEVAL_ONLY`, and `WITHHOLD`; the attestation cannot bypass other hard-deny conditions. Decision record: `docs/direct-card-safe-to-disclose-authority-decision-v1.md`.
- `accepted_design`: Future attestations require dedicated Engine-owned state and exact-binding validation; existing memories are unasserted and withhold. No backfill or inference from legacy policy/provenance/telemetry is allowed, and assert/revoke must not be ordinary agent-callable memory actions. D.3-D.3 implements this authority source; H2-E now consumes it in the production DIRECT_CARD source path. Runtime/deployment/config/DB/data work remains separately authorized.
- `current_fact`: D.3-D.3 source implementation is **`IMPLEMENTED / REPOSITORY-TESTED`** after the same-stage corrective patch. `lib/db/schema.js` defines the Engine-only `disclosure_attestations` table; the exact validator/provider, Canonical-source-derived Owner projection, and host-authenticated `/memory-disclosure` command are implemented without AutoRecall wiring. The actual artifact binding is surface `DISCLOSURE_CARD`, `projection_kind=legacy_memory_card_v1`, and Owner adapter `owner_attestable_canonical_card_v1`; no fabricated `projection_kind=DISCLOSURE_CARD` is used. The corrective patch closes both prior findings: the command registration contract and the generic placeholder Owner preview. Decision record: `docs/direct-card-owner-attested-authority-source-implementation-v1.md`.
- `historical_record`: D.3-D.3 used temporary/in-memory test databases only and did not deploy, create real attestation state, enable AutoRecall, or migrate production AutoRecall. Its historical pre-H2-E source status is retained in `docs/direct-card-owner-attested-authority-source-implementation-v1.md`.
- `current_fact`: D.3-D.4-H2-E is **`PASS / SOURCE IMPLEMENTED / REPOSITORY-TESTED`**. The host contract was verified read-only at OpenClaw H2 source commit `2e67ab06b6f7f1cf7655d0a8d508fc2a2ad78176`, which exposes `PluginHookBeforePromptBuildEvent.senderIsOwner?: boolean`; the deployed H2 correction is `755ff5d396102c5a93baacf2d3bf6187a4fea713`. The source implementation record is `docs/direct-card-pre-prompt-owner-audience-consumer-implementation-v1.md`.
- `current_fact`: D.3-D.4 / OpenSpec 4.3 is **`PASS / RUNTIME QUALIFIED / CLOSED`**. The accepted runtime record is `docs/direct-card-runtime-qualification-v1.md`; it proves the same Owner session/memory/query causal chain `attestation absent → RETRIEVAL_ONLY/WITHHOLD/injected=0`, active exact attestation → `CARD_DISCLOSABLE/DISCLOSE_CARD/injected=1`, and revoke → `RETRIEVAL_ONLY/WITHHOLD/injected=0`. The final conservative baseline is `autoRecall.enabled=false`, `cardFirstRuntime.enabled=false`, `agentAllowlist=["main"]`, `triggerAllowlist=["user"]`, `chatTypeAllowlist=["interactive_user_chat"]`, `messageRoleAllowlist=["user"]`, `sessionAllowlist=[]`, `topK=3`, `timeoutMs=8000`, active attestations `0`, and Gateway `READY`. The memory-engine qualification source is `ae95fa25fed03c4fb414d38db9e010c0157c0a15`; the original qualified OpenClaw H2 deployed source was `755ff5d396102c5a93baacf2d3bf6187a4fea713`. OpenSpec 4.4 remains unchecked and raw disclosure remains out of scope.
- `current_fact`: The follow-up finding **`EXPLICIT_MEMORY_TOOL_DISCLOSURE_BOUNDARY_NOT_UNIFIED` is `CLOSED / RUNTIME QUALIFIED / PASS`** as of 2026-08-25. Active OpenClaw is `ba0edf98d1617b92296e7b834ee7a5a82cc2a18e`; the deployed memory-engine explicit-tool target files match source `cd80a25f089a7eb7b5e0d89c476fb4dc86b0e4cb` exactly: `lib/tools/register-memory-engine-tools.js` SHA-256 `b3000993272e22536a9537c27f158e57dc2c1a8cdba88f8af1210e628e2372e5` and `lib/tools/memory-engine-actions.js` SHA-256 `a36e6f68e76fb74b263a23161e5aec234b3de4def3f61eb5f685cfb8319ab166`. Gateway is `READY` with connectivity probe `ok`; the conservative configuration remains `autoRecall.enabled=false`, `topK=3`, `cardFirstRuntime.enabled=false`, `sessionAllowlist=[]`, `agentAllowlist=["main"]`, `triggerAllowlist=["user"]`, `chatTypeAllowlist=["interactive_user_chat"]`, and `messageRoleAllowlist=["user"]`. `memory_engine_search` is the accepted model-callable **bounded untrusted retrieval** surface and returns the specialized `{ results }` projection without the Hybrid diagnostic envelope or arbitrary internal candidate fields. `memory_engine_get` is **Owner-only explicit full-content retrieval**: exact current conversational Owner authority reaches the real get executor, while direct Gateway/operator `tools.invoke` receives fixed `MEMORY_GET_OWNER_AUTH_REQUIRED` with no memory payload. OpenClaw preserves its existing Gateway/core Owner semantics through the generic `senderIsOwner` bit while the plugin factory receives Owner authority only through the separate conversational `pluginToolSenderIsOwner` seam. The accepted evidence is `docs/explicit-memory-tool-runtime-qualification-v1.md`. This closure does not reopen DIRECT_CARD 4.3, does not start OpenSpec 4.4, and does not productionize `INTERNAL_AGENT_CONTEXT`.
- `current_fact`: As of 2026-08-26, the accepted local runtime baseline is **OpenClaw `2026.7.1-2` with patched host runtime `ded67f3cd7fde6ae19798a402c178e2d76fc2c71` plus memory-engine `1.0.0` from source HEAD `d201c8e5246773ba5e26dc8a959fa93beec9661d`**. Gateway is `READY`; direct Gateway/operator `memory_engine_get` remains fail-closed with `MEMORY_GET_OWNER_AUTH_REQUIRED`, while a real WebChat Owner turn records `senderIsOwner=true` and reaches the ordinary get executor (`error="not found"`) for a guaranteed-missing id. The 1.0.0 deployment preserved the Engine DB byte-for-byte during install, kept active disclosure attestations at `0`, and retained `autoRecall.enabled=false` and `cardFirstRuntime.enabled=false`. Historical 0.8.22 backup directories were moved out of the active plugin-discovery tree; the duplicate `memory-engine` discovery warning is absent after the single cleanup restart. This supersedes older runtime-version statements but does not reopen DIRECT_CARD 4.3 or authorize OpenSpec 4.4.
- `current_fact`: **memory-engine Benchmark v1 B3 is `PASS_WITH_FINDINGS / OFFLINE BASELINE RECORDED`**. The first official LongMemEval-S run uses `production_hybrid_lexical_session_v1`, `top_k=50`, and cleaned dataset SHA-256 `d6f21ea9d60a0d56f34a05b609c79c88a451d2ae03597821ea3d5a9678c3a442`. The validator accepts official empty-string turn contents while rejecting non-string content; the runner preserves duplicate session-id corpus occurrences and follows upstream retrieval exclusions (30 abstention plus 51 additional no-user-target cases after overlap), leaving 419 scored cases. Overall lexical baseline: recall-any `@1=0.5107`, `@5=0.7876`, `@10=0.9045`, `@50=0.9952`; recall-all `@1=0.1289`, `@5=0.4821`, `@10=0.6372`, `@50=0.9761`; NDCG-any `@10=0.6256`; mean search latency `9.49 ms`. `knowledge-update` is strongest while `single-session-preference` is weakest; multi-session and temporal families show a material any-vs-all evidence gap. This is an offline lexical/fusion baseline only; vector remains an explicit empty backend, live OpenClaw/memory-engine state is untouched. LoCoMo B5 has now recorded its lexical and semantic baselines below; AML remains a later integration. Benchmark evidence does not authorize live runtime/config/DB mutation.
- `current_fact`: **memory-engine Benchmark v1 B4 is `PASS_WITH_FINDINGS / OFFLINE BASELINE RECORDED / BASELINE FROZEN / CLOSED`**. B4-S2 execution passed under `production_hybrid_semantic_session_v1` at source commit `b640c3547622a646d8962e27b20f0d5ac5a13cc1`, dataset SHA-256 `d6f21ea9d60a0d56f34a05b609c79c88a451d2ae03597821ea3d5a9678c3a442`, SiliconFlow `Qwen/Qwen3-Embedding-4B`, model revision `unavailable/unpinned`, dimension `2560`, Canonical projection v1/2000 chars, `top_k=50`, and `lexicalConfidenceThreshold=0.7`. The run recorded 500 total / 419 scored / 81 skipped cases, output SHA-256 `de62cf40fe21485bf99a4f17129efa6097938a29ace8e88b78b2c2ca90c86cb8`, and final cache SHA-256 `c1b8f292e8e2b95581f09b4526e2ce4c38517c9cfc1b93e0ffbde4b261319db2`. Overall semantic deltas versus frozen B3 were Recall-any@5 `+0.0263`, Recall-all@5 `+0.0406`, Recall-all@10 `+0.0453`, and NDCG-any@10 `+0.0297`, with mean retrieval latency `205.11 ms` versus `9.49 ms`. Semantic interpretation is **`USEFUL BUT INSUFFICIENT`**: this is offline evidence only, not production/runtime qualification, and does not establish a solution for preference or multi-evidence coverage. Output/cache remain temporary and outside repository authority; B5-I1 LoCoMo dataset/metric contract is now `REPO-TESTED / CONTRACT FROZEN`; the historical B5-S1 record is preserved below, and the current B5-S1-v2 fact is the current B5 authority. Benchmark evidence does not authorize production tuning or runtime/config/DB/deployment mutation.
- `current_fact`: **Benchmark v1 RH1 is `INSUFFICIENT / OFFLINE EXPERIMENT RECORDED / CLOSED`**. RH1-S2 execution passed under `production_hybrid_semantic_query_instruction_session_v1` at source commit `d2d2fbd7e8eba77cd416923f44815e1289d4d7df`, dataset SHA-256 `d6f21ea9d60a0d56f34a05b609c79c88a451d2ae03597821ea3d5a9678c3a442`, SiliconFlow `Qwen/Qwen3-Embedding-4B`, model revision `unavailable/unpinned`, and dimension `2560`. The fixed query instruction is version `query_embedding_instruction_v1`, with SHA-256 `e3440313f1178547f222be0bae9e1071cfdabaad86188f8c6af6e94f7b049f41`; only the vector query input changed, while document/corpus and lexical inputs plus B3/B4 remained frozen. The run recorded 500 total / 419 scored / 81 skipped cases, 19,933 corpus embeddings, 419 query embeddings, 418 provider calls, 19,934 cache hits, and vector attempted/skipped/error `419/0/0`. Output SHA-256 is `fd102cf0983d0189855496111059158a04db1a7010b8d4f7dbce9294effc3fd6`; final cache SHA-256 is `ba721b841e3ab71eebba9191e939fab1d23c7f56f74822d8e68c6e4c63e365f6`. Against frozen B4, Recall-any@5 improved `0.8138 → 0.8234` (`+0.0095`), below the pre-frozen `+0.0100` threshold; temporal-reasoning improved All@10 by `+0.0236`, while single-session-preference Any@5 fell by `-0.0333` and multi-session All@10 was unchanged. Case-level first-rank comparison was `32/5/382` improved/regressed/unchanged; RH1 does not solve preference or multi-session all-evidence coverage. Artifacts remain temporary and are not repository authority; B4 remains the frozen semantic baseline authority, and Benchmark evidence is not production authority. H2-I1/I2 are `REPO-TESTED`, H2-S1 is `PASS / REAL-PROVIDER CHAIN QUALIFIED`, and the current H2 authority is `INSUFFICIENT / CLOSED` with H2-S2-R2 `DO NOT RUN / NOT AUTHORIZED`; B5-I1 LoCoMo dataset/metric contract is `REPO-TESTED / CONTRACT FROZEN`; the historical B5-S1 lexical record is preserved below, B5-S1-v2 remains the frozen lexical comparison authority, B5 semantic v2 is `PASS_WITH_FINDINGS / OFFLINE BASELINE RECORDED / BASELINE FROZEN / CLOSED`, and B5 overall is `PASS_WITH_FINDINGS / CROSS-DATASET GENERALIZATION RECORDED / CLOSED`. B6-I1/I2/I3 are `CLOSED`; B6-S1 local synthetic HTTP/Docker qualification is `PASS / CLOSED / NO REAL PROVIDER`; B6-S2 official smoke and B6-S3 full evaluation remain unauthorized.
- `accepted_design`: D.3-D.4-H1 remains **`PASS_WITH_FINDINGS / CONTRACT DESIGN FROZEN`** as a historical design record. A later authoritative-source correction verified that the `v2026.6.9` source does contain the CLI prompt-build path and that H2 covers it. The H2-E consumer uses only exact `event?.senderIsOwner === true`; it does not infer Owner from sender/session/channel/config or reuse authority across turns. Design history remains in `docs/openclaw-pre-prompt-owner-audience-contract-design-v1.md`.

- `historical_record`: H2-D1 recorded H2-I1/I2 as `REPO-TESTED / PRE-SANITY HARDENED` and H2-S1 as `PASS / REAL-PROVIDER CHAIN QUALIFIED` for `production_hybrid_semantic_bounded_multi_query_session_v1`. That pre-sanity checkpoint is superseded by the H2-D2 final closeout below; the temporary artifacts were never repository authority.

- `current_fact`: **Benchmark v1 H2 is `INSUFFICIENT / FULL EVALUATION NOT COMPLETED / PLANNER CONTRACT NOT DATASET-ROBUST / OFFLINE EXPERIMENT RECORDED / CLOSED`**. H2-S1 remains `PASS / REAL-PROVIDER CHAIN QUALIFIED`, while H2-S2 initial execution was `EXECUTION_INTERRUPTED / INCOMPLETE` with provider-capable execution `1/1 consumed` and no full output. H2-S2-R1 passed preflight at source commit `7c404efcf7a02c8867020a3ee8fe4c9fae1d8117`, consumed `1/1`, and failed closed with `COMMAND_RC=1 / PLANNER_OUTPUT_CONTRACT_VIOLATION` when the strict planner parser rejected a real planner query duplicating the production query at dataset index `405` / scored index `382` (`22d2cb42`, `knowledge-update`). The six H2-S2 numerical gates are **`NOT EVALUABLE`**: no complete overall, family, or case-level metrics exist, and the H2 insufficiency is not a numerical threshold result. Partial caches are evidence only: final embedding cache SHA-256 `50e9f830d93e07b3b5d53d19c267af90ff4f55c3ed7b503e4d27f645fe3ba170` with `17118` entries and final query-plan cache SHA-256 `5ae8f0de090cb826f09778d4f363b3004d3b0fcf0be2ae9e78942ba37a91ab3c` with `381` entries; both are dimension/schema-valid temporary artifacts, not baseline authority. H2-S2-R2 is `DO NOT RUN / NOT AUTHORIZED`; H2 is not reopened or repaired in place. B4 remains the frozen semantic baseline authority, B5-I1 LoCoMo dataset/metric contract is `REPO-TESTED / CONTRACT FROZEN`; that H2 snapshot predates B5-S1 and is superseded by the current B5 fact below, and benchmark evidence is not production authority.
- `historical_record`: **Historical B5-S1 v1 checkpoint: `PASS_WITH_FINDINGS / OFFLINE LEXICAL BASELINE RECORDED / BASELINE FROZEN / CLOSED`**, with `B5-S1 v1 = HISTORICAL LEXICAL EVIDENCE / TIME PROVENANCE INCOMPLETE / NOT STRICT SEMANTIC A/B AUTHORITY`. B5-I1 contract and B5-I2/I2a/I2b source work are `REPO-TESTED`. The historical profile is `production_hybrid_lexical_dialog_locomo_v1` at source commit `d45ea0de2b9b33a222118ed312c1ff74ca4e84d6`, upstream commit `3eb6f2c585f5e1699204e3c3bdf7adc5c28cb376`, dataset SHA-256 `79fa87e90f04081343b8c8debecb80a9a6842b76a7aa537dc9fdf651ea698ff4`, `benchmark_now_sec=1705066861`, and `top_k=50`. The R1 output is temporary at `/tmp/memory-engine-benchmark-v1/b5-s1-r1-d45ea0de-locomo-lexical-output.json` with SHA-256 `92248dc1d9134130904098491d93daabffda2cc40b66fbd26057705dfbe41d91`; it is not repository authority. The run records `1,986` cases / `1,978` retrieval cases, `1,972/14` strict and `1,978/8` sensitivity scored/skipped, `10` conversation corpora, vector mode `disabled_empty_backend`, and host-manager mode `disabled`. Strict dialog primary Recall-any@10 is `0.321501`, Recall-all@10 `0.261156`, and NDCG-any@10 `0.211939`; multi-hop is the clearest weak category and temporal is strongest, so quality is `PASS_WITH_FINDINGS`. This historical checkpoint is preserved for lexical evidence only and is superseded as strict A/B authority; its metrics and execution evidence remain unchanged.
- `current_fact`: **B5-S1-v2 = `PASS_WITH_FINDINGS / BASELINE FROZEN / CLOSED`** under the `REPO-TESTED` time-frozen source profile `production_hybrid_lexical_dialog_locomo_time_frozen_v2` at source commit `b4a53522bba7a1314b2c99f1fd566872e6e5e8d4`. The dataset SHA-256 is `79fa87e90f04081343b8c8debecb80a9a6842b76a7aa537dc9fdf651ea698ff4`, `benchmark_now_sec = materialization_now_sec = search_now_sec = 1705066861`, and `top_k=50`. The temporary output `/tmp/memory-engine-benchmark-v1/b5-s1-v2-b4a53522-locomo-lexical-output.json` has SHA-256 `9d6c4c02a5fff3841f98552f5d7ee087c57448bc8a917606a047ac395e222f06` and size `31,280,002` bytes; the temporary log `/tmp/memory-engine-benchmark-v1/b5-s1-v2-b4a53522.log` has SHA-256 `7b066eea1c314f9e93d84deb86cb1087a8b5c1c8e5b25c40e1bc04caf5b86af7`, and `RC=0`. Strict is `1,972` scored / `14` skipped and sensitivity/retrieval is `1,978` scored / `8` skipped. Strict dialog metrics are Recall-any@1 `0.1546653144`, @5 `0.2728194726`, @10 `0.3220081136`, @50 `0.4239350913`, Recall-all@5 `0.2276876268`, @10 `0.2621703854`, @50 `0.3306288032`, NDCG-any@10 `0.2135986258`, MRR `0.2093299195`, mean first relevant rank `7.9629186603`, zero-hit@50 `1,136/1,972`, and All@10 failures `1,455/1,972`; mean retrieval latency is `15.4559368468 ms`, corpus build latency total `754.702548 ms`, and retrieval latency total `30,571.843083 ms`. Every scoreable diagnostic has `search_now_sec=1705066861`, with missing `0` and mismatch `0`. Lexical confidence count is `1,978`, non-finite `0`, min/max `0.3571/0.6900`, mean `0.5010601112`, p50/p90/p95/p99 `0.5063/0.5563/0.5786/0.6275`, below `0.7` `1,978`, equal/above `0.7` `0`. The frozen semantic v2 vector invariant is attempted/skipped/error `1,978/0/0`, every attempted case must use LanceDB at `lancedb_search` with `vector_in_fusion=true`, official vector-skipped cases must not execute query embedding/vector search, host-manager fallback is `0`, and silent lexical-only degradation is forbidden. Versus historical v1, retrieved order changed in `434/1,978` cases; first-rank comparison is `22 improved / 36 regressed / 1,920 unchanged`; Recall-any@10 delta `+0.000507`, Recall-all@10 delta `+0.001014`, NDCG-any@10 delta `+0.001660`, and Recall-any/Recall-all@50 delta `0`. These are clock-only findings and do not authorize retrieval-heuristic tuning. B5-S1 v1 remains historical lexical evidence with incomplete time provenance; B5-S1-v2 remains the frozen lexical comparison authority. Output and log remain temporary artifacts; long-term authority is the committed provenance, metrics, invariants, and adjudication. No provider, semantic baseline, or runtime/live-data operation is authorized by this record.
- `current_fact`: **B5 semantic v2 is `PASS_WITH_FINDINGS / OFFLINE BASELINE RECORDED / BASELINE FROZEN / CLOSED`** for `production_hybrid_semantic_dialog_locomo_time_frozen_v2` at source commit `dfb41ea866646cbe556e3e6c3c03782da52de216`. Dataset SHA-256 is `79fa87e90f04081343b8c8debecb80a9a6842b76a7aa537dc9fdf651ea698ff4`, fixed clock is `1705066861`, strict/sensitivity are `1,972/14` and `1,978/8`, output SHA-256 is `eb4b29c56435297f7a376f18647ca44cce4659b38a8b3fa1e980c853082b1829`, and vector attempted/skipped/error is `1,978/0/0` with LanceDB in production fusion for every retrieval case. Five of six semantic gates passed and all regression guards passed; Multi-hop Recall-all@10 was the only unmet gate. Mean retrieval latency was `364.761142 ms` versus lexical v2 `15.455937 ms` (`23.60×`). B5 overall is **`PASS_WITH_FINDINGS / CROSS-DATASET GENERALIZATION RECORDED / CLOSED`**. B6-I1/I2/I3 are **`CLOSED`**, B6-S1 local synthetic HTTP/Docker qualification is **`PASS / CLOSED / NO REAL PROVIDER`**, and B6-S2 official smoke/B6-S3 full evaluation remain unauthorized. Benchmark evidence is not runtime or production authority.
- `historical_record`: **Benchmark v1 B6-I1 is `CLOSED`, B6-I2a is `CLOSED`, B6-I2 is `SOURCE IMPLEMENTATION REPO-TESTED / CLOSED`, and B6-I3 is `SOURCE IMPLEMENTATION REPO-TESTED / CLOSED` / NO OFFICIAL EXECUTION** against AML public source commit `1b8142bfe0f20f1c5218d6b554aa0012de34e504`. `memory_engine_aml_adapter_v1` retains pinned Add/Search schema validation, persistent `request_id` idempotency, per-`user_id` serialized operations, physically isolated temporary Core/Engine/vector-path runtimes, production `autoRouteCategory()`/`catParams()` ingestion metadata, production `hybridSearch()` ranking, `confidence.min=0.15`, lexical semantic gate `0.7`, `top_k<=100`, exact-question retrieval with Search `options` excluded from the query, host-manager fallback forbidden, and the frozen `production_hybrid_text_240_v1` evidence surface. The shared benchmark runtime helper's `minConfidence` option still defaults to `0`, so B3-B5 behavior remains unchanged. B6-I2a adds the benchmark-only `memory_engine_aml_semantic_backend_v1` with explicit provider injection, frozen 2,560-dimensional SiliconFlow model identity, the existing embedding-cache contract, real temporary LanceDB `chunks` storage, deterministic row reuse/repair, and operation-scoped cache/table/connection close semantics. B6-I2b adds durable `pending → Core/FTS ensure → vector when required → ready` Add ordering, in-place ledger state migration, retained-root manifest/ownership validation, lazy existing-user reopen, confidence-0 pending visibility filtering, and child-process crash-boundary reconciliation without changing production Hybrid semantics. B6-I3 adds the benchmark-local `GET /health`, `POST /add`, and `POST /search` Node HTTP transport, env-only semantic provider wiring, bounded JSON/error contracts, graceful retained-root shutdown, and Node 24 non-root Docker packaging. Focused B6-I3 transport coverage is repository-tested; the complete Benchmark v1 family is recorded from the fresh Node 24 run. All tests use temporary data and deterministic fake embeddings; no real provider, AML service, official AML execution, public deployment, or live runtime/data mutation occurred. B6-S1 local synthetic HTTP/Docker qualification is next and uses no real provider; B6-S2 official smoke/B6-S3 full evaluation remain unauthorized. No Docker submission, Gateway/config/plugin mutation, or leaderboard execution occurred.

- `historical_record`: **B6-I3-R1 = `SEARCH ITEM DISCLOSURE/OUTPUT BOUNDARY HARDENED / REPO-TESTED`**. Search item output uses the exact allowlist `{id, content, score?, created_at?}`; unknown enumerable fields fail closed with `adapter_search_response_invalid`, and numeric `score`/`created_at` values must be finite while the existing `created_at=null` contract remains valid. The HTTP focused suite is `12/12 PASS`; the complete Benchmark v1 family is `182 PASS / 4 SKIP / 0 FAIL`; B6-I3 remains `SOURCE IMPLEMENTATION REPO-TESTED / CLOSED`, B6-S1 remains `NEXT / LOCAL SYNTHETIC HTTP/DOCKER QUALIFICATION / NO REAL PROVIDER`, and B6-S2/S3 remain unauthorized. No provider, official AML service, Docker deployment, or runtime/live data operation occurred.

- `historical_record`: **B6-I3-R2 = `SOURCE IMPLEMENTATION REPO-TESTED / DOCKER BUILD QUALIFIED`**. B6-S1 Phase A is `PASS`; its initial Phase B Docker build was `FAIL_WITH_FINDINGS` because the dependencies stage lacked the Python/build toolchain required for `better-sqlite3@11.10.0` on Node 24. The bounded correction installs `python3`, `make`, and `g++` with `--no-install-recommends` in the discarded dependencies stage, removes `/var/lib/apt/lists/*`, and leaves the Node 24 runtime stage to copy only the resulting `node_modules`. Docker contract, HTTP, service/runtime, full Benchmark family, static, and diff checks pass; the real image build passes, both native modules load in the image, the default container UID is `1000`, and the final image has no `python3`, `make`, `g++`, or apt cache. B6-I3 is `CLOSED`; B6-S1 is `RETRY READY / PHASE B→D REMAINING` and is not closed. No provider, official AML execution, deployment, live runtime/data operation, Gateway/config/plugin change, or tag/push operation occurred.

- `historical_record`: **B6-I3-R3 = `SOURCE IMPLEMENTATION REPO-TESTED / DOCKER RUNTIME CLOSURE QUALIFIED`**. B6-I3-R2 is `PASS / DOCKER NATIVE DEPENDENCY PACKAGING FIXED`; B6-S1 Phase B is `PASS`, while its initial Phase C attempt was `FAIL_WITH_FINDINGS` because runtime module closure omitted root `query-utils.js`, causing `ERR_MODULE_NOT_FOUND` for `/app/query-utils.js`. R3 adds only `COPY query-utils.js ./` to the runtime stage and freezes the minimal runtime COPY surface (`package.json`, `package-lock.json`, `query-utils.js`, `bin/`, `lib/`, plus builder-produced `node_modules`); `COPY . .` remains forbidden. The rebuilt image passes `SERVICE_IMPORT_OK`, `NATIVE_RUNTIME_OK`, and non-root UID validation. At that checkpoint B6-I3 was `CLOSED` and B6-S1 was `RETRY READY / PHASE C→D REMAINING`; the later B6-S1 qualification below supersedes that retry-ready status. Real provider, official AML, deployment, live runtime/data, Gateway/config/plugin, and tag/push operations were not performed.

- `current_fact`: **B6-S1 Local Synthetic Qualification is `PASS / CLOSED`** at repository source `b2063f4531a5b0136de33ffee7e31c5235017028`. Phase C used the real Docker image, benchmark HTTP service, actual temporary SQLite/LanceDB state, and a local deterministic 2,560-dimensional fake embedding endpoint only: `/health` passed, unauthenticated Search returned `401 authentication_required`, Add/Search returned the expected memory, the same `request_id` retry remained idempotent with provider calls fixed at `2`, and unknown-user Search returned empty without allocating a second user data plane. Phase D stopped the first container gracefully with exit `0`, preserved `manifest.json`, Core/Engine SQLite, embedding cache, and user manifest in the named volume, then started a fresh container on the same volume and recovered the same memory without re-Add; provider calls remained `2`, proving persistent query-embedding cache reuse across restart. The second container also stopped with exit `0`, and all synthetic Docker containers/network/volume were cleaned up after evidence capture. Real SiliconFlow provider calls, official AML execution, public deployment, live OpenClaw Core/Engine/LanceDB mutation, Gateway/config/plugin changes, and tag/push operations were all `0`. B6-I3 remains `CLOSED`; B6-S2 official AML smoke and B6-S3 full evaluation remain `NOT AUTHORIZED`.

- `current_fact`: **Structured event-time is not yet a qualified production temporal channel.** P46 defines `memory_event_times` as an Engine-owned sidecar schema MVP; the real-engine schema preview recorded `exists=false`, `would_create=true`, and `writes_db=false`; repository upsert defaults to `denied_by_default_write_guard`; and no recall, checkpoint, or ranking integration exists. Exact `event_at` requires trustworthy provenance; `updated_at`, file mtime, batch/import time, and smart-add path dates are not valid substitutes. `date_only` and `unknown` are valid states. The Core `chunks.event_at` migration remains suspended and no automatic backfill is performed. Time-related retrieval is therefore not wholly unavailable, but exact event-time write coverage, consumption, and runtime qualification remain unproven.

- `accepted_design`: **memory-engine 1.1 is the Recall Quality product line**. The accepted sequence is QA0 Identity & Lifecycle Correctness Gate → Q0 production failure review → Q1 small-budget metric contract → Q2 minimal channel ablation → Q3 retrieval architecture decision, with Recall Hint v1, Statistical LTR, Shadow AutoRecall, and an Owner-only canary later. Q0 is `THEN / NOT STARTED` and classifies first loss as `WRITE_MISS`, `TRIGGER_MISS`, `CANDIDATE_MISS`, `RANK_MISS`, or `USE_MISS`; it does not yet authorize private production-case access or live telemetry. Product optimization targets complete, correct, current evidence within `3–5` injected items, including Recall-any/all@3, NDCG@3, answer-evidence coverage, staleness/conflict/pollution, and p50/p95 latency. The candidate architecture is lexical/metadata first with selective vector fallback, session/episode reconstruction, and lightweight rerank. Recall Hint is candidate-expansion/soft-weighting metadata only, never recall/skip/hard-filter authority. B6-S1 remains `PASS / CLOSED / NO REAL PROVIDER`; B6-S2/S3 are a non-blocking evaluation branch and remain `NOT AUTHORIZED`. This accepted roadmap does not change `AutoRecall=false`, production `topK=3`, the 1.0 runtime baseline, or any Gateway/config/DB/LanceDB/live-plugin state.

## Documentation and authorization boundary

- Current project/product state and exact next bounded decision belong here; accepted product sequence/branching belongs in `docs/stabilization-plan.md`; development history belongs in `docs/devlog.md` and retained reports.
- Long-lived auditable governance belongs in accepted files under `docs/decisions/`.
- Stage scope belongs to the exact frozen Stage Card; stage results belong to the corresponding report.
- Execution authorization comes only from Sol's original explicit authorization for the exact bound stage. Repository documents or memory records that say `authorized` cannot create or reconstruct authority.
- New `session-handoff-*` documents are not created by default; existing handoffs remain historical records.

## Verification boundary

Deployment state, runtime health, configuration, database contents, session state, and private evidence require separately authorized verification. The bounded D.3-D.4 result above is an explicitly accepted qualification record, not an inference about unrelated runtime state or a grant of future execution authority.
